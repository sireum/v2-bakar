Model(Some(type_conversion.adb),Vector(),Vector(PackageDecl(Some(NameDefinition(Type_Conversion)),Vector(),List(TypeAliasDecl(NameDefinition(standard:
  Some(type_conversion.adb)
  List()
  Vector(PackageDecl(Some(NameDefinition(Type_Conversion)),Vector(),List(TypeAliasDecl(NameDefinition(standard::boolean),Vector(),NamedTypeSpec(NameUser
    PackageDecl(Some(NameDefinition(Type_Conversion)),Vector(),List(TypeAliasDecl(NameDefinition(standard::boolean),Vector(),NamedTypeSpec(NameUser(Boolea
      Some(NameDefinition(Type_Conversion))
        NameDefinition(Type_Conversion)
      List()
      List(TypeAliasDecl(NameDefinition(standard::boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List())), TypeAliasDecl(NameDefinition(standard::integer
        TypeAliasDecl(NameDefinition(standard::boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List()))
          NameDefinition(standard::boolean)
          List()
          NamedTypeSpec(NameUser(Boolean),List())
            NameUser(Boolean)
            List()
        TypeAliasDecl(NameDefinition(standard::integer),Vector(),NamedTypeSpec(NameUser(Integer),List()))
          NameDefinition(standard::integer)
          List()
          NamedTypeSpec(NameUser(Integer),List())
            NameUser(Integer)
            List()
        TypeAliasDecl(NameDefinition(standard::natural),Vector(),NamedTypeSpec(NameUser(Integer),List()))
          NameDefinition(standard::natural)
          List()
          NamedTypeSpec(NameUser(Integer),List())
            NameUser(Integer)
            List()
        TypeAliasDecl(NameDefinition(standard::positive),Vector(),NamedTypeSpec(NameUser(Integer),List()))
          NameDefinition(standard::positive)
          List()
          NamedTypeSpec(NameUser(Integer),List())
            NameUser(Integer)
            List()
        TypeAliasDecl(NameDefinition(standard::float),Vector(),NamedTypeSpec(NameUser(Float),List()))
          NameDefinition(standard::float)
          List()
          NamedTypeSpec(NameUser(Float),List())
            NameUser(Float)
            List()
        TypeAliasDecl(NameDefinition(universal_integer),Vector(),NamedTypeSpec(NameUser(Integer),List()))
          NameDefinition(universal_integer)
          List()
          NamedTypeSpec(NameUser(Integer),List())
            NameUser(Integer)
            List()
        ProcedureDecl(NameDefinition(Convert),Vector(),Vector(),List(ParamDecl(Some(NamedTypeSpec(NameUser(Float),Vector())),NameDefinition(Y),Vector()), Para
          NameDefinition(Convert)
          List()
          List()
          List(ParamDecl(Some(NamedTypeSpec(NameUser(Float),Vector())),NameDefinition(Y),Vector()), ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),Na
            ParamDecl(Some(NamedTypeSpec(NameUser(Float),Vector())),NameDefinition(Y),Vector())
              Some(NamedTypeSpec(NameUser(Float),Vector()))
                NamedTypeSpec(NameUser(Float),Vector())
                  NameUser(Float)
                  List()
              NameDefinition(Y)
              List()
            ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(X),Vector())
              Some(NamedTypeSpec(NameUser(Integer),Vector()))
                NamedTypeSpec(NameUser(Integer),Vector())
                  NameUser(Integer)
                  List()
              NameDefinition(X)
              List()
          None
          ImplementedBody(List(),List(ActionLocation(Some(NameDefinition(l1)),Vector(),AssignAction(Vector(),NameExp(NameUser(X)),:=,NameExp(NameUser(Y)))), Jum
            List()
            List(ActionLocation(Some(NameDefinition(l1)),Vector(),AssignAction(Vector(),NameExp(NameUser(X)),:=,NameExp(NameUser(Y)))), JumpLocation(Some(NameDefi
              ActionLocation(Some(NameDefinition(l1)),Vector(),AssignAction(Vector(),NameExp(NameUser(X)),:=,NameExp(NameUser(Y))))
                Some(NameDefinition(l1))
                  NameDefinition(l1)
                List()
                AssignAction(Vector(),NameExp(NameUser(X)),:=,NameExp(NameUser(Y)))
                  List()
                  NameExp(NameUser(X))
                    NameUser(X)
                  NameExp(NameUser(Y))
                    NameUser(Y)
              JumpLocation(Some(NameDefinition(l2)),Vector(),ReturnJump(Vector(),None))
                Some(NameDefinition(l2))
                  NameDefinition(l2)
                List()
                ReturnJump(Vector(),None)
                  List()
                  None
            List()

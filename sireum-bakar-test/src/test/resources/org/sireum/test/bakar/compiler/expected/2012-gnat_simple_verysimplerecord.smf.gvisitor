Model(Some(verysimplerecord.adb),Vector(),Vector(PackageDecl(Some(NameDefinition(SimpleRecord)),Vector(),List(RecordDecl(NameDefinition(Rec),Vector(),
  Some(verysimplerecord.adb)
  List()
  Vector(PackageDecl(Some(NameDefinition(SimpleRecord)),Vector(),List(RecordDecl(NameDefinition(Rec),Vector(),List(),List(),Vector(AttributeDecl(NameDef
    PackageDecl(Some(NameDefinition(SimpleRecord)),Vector(),List(RecordDecl(NameDefinition(Rec),Vector(),List(),List(),Vector(AttributeDecl(NameDefinition
      Some(NameDefinition(SimpleRecord))
        NameDefinition(SimpleRecord)
      List()
      List(RecordDecl(NameDefinition(Rec),Vector(),List(),List(),Vector(AttributeDecl(NameDefinition(A),Vector(),Some(NamedTypeSpec(NameUser(Integer),List()
        RecordDecl(NameDefinition(Rec),Vector(),List(),List(),Vector(AttributeDecl(NameDefinition(A),Vector(),Some(NamedTypeSpec(NameUser(Integer),List())),No
          NameDefinition(Rec)
          List()
          List()
          List()
          Vector(AttributeDecl(NameDefinition(A),Vector(),Some(NamedTypeSpec(NameUser(Integer),List())),None), AttributeDecl(NameDefinition(B),Vector(),Some(Nam
            AttributeDecl(NameDefinition(A),Vector(),Some(NamedTypeSpec(NameUser(Integer),List())),None)
              NameDefinition(A)
              List()
              Some(NamedTypeSpec(NameUser(Integer),List()))
                NamedTypeSpec(NameUser(Integer),List())
                  NameUser(Integer)
                  List()
              None
            AttributeDecl(NameDefinition(B),Vector(),Some(NamedTypeSpec(NameUser(Integer),List())),None)
              NameDefinition(B)
              List()
              Some(NamedTypeSpec(NameUser(Integer),List()))
                NamedTypeSpec(NameUser(Integer),List())
                  NameUser(Integer)
                  List()
              None
        GlobalVarDecl(NameDefinition(@@RG),Vector(),Some(NamedTypeSpec(NameUser(Rec),Vector())))
          NameDefinition(@@RG)
          List()
          Some(NamedTypeSpec(NameUser(Rec),Vector()))
            NamedTypeSpec(NameUser(Rec),Vector())
              NameUser(Rec)
              List()
        ProcedureDecl(NameDefinition(Dummy),Vector(),Vector(),List(),Some(NamedTypeSpec(NameUser(Boolean),Vector())),false,ImplementedBody(List(),List(JumpLoc
          NameDefinition(Dummy)
          List()
          List()
          List()
          Some(NamedTypeSpec(NameUser(Boolean),Vector()))
            NamedTypeSpec(NameUser(Boolean),Vector())
              NameUser(Boolean)
              List()
          ImplementedBody(List(),List(JumpLocation(Some(NameDefinition(l1)),Vector(),ReturnJump(Vector(),Some(LiteralExp(BOOLEAN,true,true))))),Vector())
            List()
            List(JumpLocation(Some(NameDefinition(l1)),Vector(),ReturnJump(Vector(),Some(LiteralExp(BOOLEAN,true,true)))))
              JumpLocation(Some(NameDefinition(l1)),Vector(),ReturnJump(Vector(),Some(LiteralExp(BOOLEAN,true,true))))
                Some(NameDefinition(l1))
                  NameDefinition(l1)
                List()
                ReturnJump(Vector(),Some(LiteralExp(BOOLEAN,true,true)))
                  List()
                  Some(LiteralExp(BOOLEAN,true,true))
                    LiteralExp(BOOLEAN,true,true)
            List()
        ProcedureDecl(NameDefinition(Add),Vector(),Vector(),List(ParamDecl(Some(NamedTypeSpec(NameUser(Rec),Vector())),NameDefinition(R),Vector())),None,false
          NameDefinition(Add)
          List()
          List()
          List(ParamDecl(Some(NamedTypeSpec(NameUser(Rec),Vector())),NameDefinition(R),Vector()))
            ParamDecl(Some(NamedTypeSpec(NameUser(Rec),Vector())),NameDefinition(R),Vector())
              Some(NamedTypeSpec(NameUser(Rec),Vector()))
                NamedTypeSpec(NameUser(Rec),Vector())
                  NameUser(Rec)
                  List()
              NameDefinition(R)
              List()
          None
          ImplementedBody(List(),List(ActionLocation(Some(NameDefinition(l2)),Vector(),AssignAction(Vector(),AccessExp(NameExp(NameUser(R)),NameUser(A)),:=,Bina
            List()
            List(ActionLocation(Some(NameDefinition(l2)),Vector(),AssignAction(Vector(),AccessExp(NameExp(NameUser(R)),NameUser(A)),:=,BinaryExp(+,AccessExp(NameE
              ActionLocation(Some(NameDefinition(l2)),Vector(),AssignAction(Vector(),AccessExp(NameExp(NameUser(R)),NameUser(A)),:=,BinaryExp(+,AccessExp(NameExp(Na
                Some(NameDefinition(l2))
                  NameDefinition(l2)
                List()
                AssignAction(Vector(),AccessExp(NameExp(NameUser(R)),NameUser(A)),:=,BinaryExp(+,AccessExp(NameExp(NameUser(R)),NameUser(A)),LiteralExp(INTEGER,1,1ii)
                  List()
                  AccessExp(NameExp(NameUser(R)),NameUser(A))
                    NameExp(NameUser(R))
                      NameUser(R)
                    NameUser(A)
                  BinaryExp(+,AccessExp(NameExp(NameUser(R)),NameUser(A)),LiteralExp(INTEGER,1,1ii))
                    AccessExp(NameExp(NameUser(R)),NameUser(A))
                      NameExp(NameUser(R))
                        NameUser(R)
                      NameUser(A)
                    LiteralExp(INTEGER,1,1ii)
              JumpLocation(Some(NameDefinition(l3)),Vector(),ReturnJump(Vector(),None))
                Some(NameDefinition(l3))
                  NameDefinition(l3)
                List()
                ReturnJump(Vector(),None)
                  List()
                  None
            List()
        ProcedureDecl(NameDefinition(Del),Vector(),Vector(),List(),None,false,ImplementedBody(List(LocalVarDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())
          NameDefinition(Del)
          List()
          List()
          List()
          None
          ImplementedBody(List(LocalVarDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(T),Vector())),List(ActionLocation(Some(NameDefinition
            List(LocalVarDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(T),Vector()))
              LocalVarDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(T),Vector())
                Some(NamedTypeSpec(NameUser(Integer),Vector()))
                  NamedTypeSpec(NameUser(Integer),Vector())
                    NameUser(Integer)
                    List()
                NameDefinition(T)
                List()
            List(ActionLocation(Some(NameDefinition(l4)),Vector(),AssignAction(Vector(),NameExp(NameUser(T)),:=,AccessExp(NameExp(NameUser(@@RG)),NameUser(A)))), 
              ActionLocation(Some(NameDefinition(l4)),Vector(),AssignAction(Vector(),NameExp(NameUser(T)),:=,AccessExp(NameExp(NameUser(@@RG)),NameUser(A))))
                Some(NameDefinition(l4))
                  NameDefinition(l4)
                List()
                AssignAction(Vector(),NameExp(NameUser(T)),:=,AccessExp(NameExp(NameUser(@@RG)),NameUser(A)))
                  List()
                  NameExp(NameUser(T))
                    NameUser(T)
                  AccessExp(NameExp(NameUser(@@RG)),NameUser(A))
                    NameExp(NameUser(@@RG))
                      NameUser(@@RG)
                    NameUser(A)
              ActionLocation(Some(NameDefinition(l5)),Vector(),AssignAction(Vector(),AccessExp(NameExp(NameUser(@@RG)),NameUser(A)),:=,BinaryExp(-,BinaryExp(-,Acces
                Some(NameDefinition(l5))
                  NameDefinition(l5)
                List()
                AssignAction(Vector(),AccessExp(NameExp(NameUser(@@RG)),NameUser(A)),:=,BinaryExp(-,BinaryExp(-,AccessExp(NameExp(NameUser(@@RG)),NameUser(B)),AccessE
                  List()
                  AccessExp(NameExp(NameUser(@@RG)),NameUser(A))
                    NameExp(NameUser(@@RG))
                      NameUser(@@RG)
                    NameUser(A)
                  BinaryExp(-,BinaryExp(-,AccessExp(NameExp(NameUser(@@RG)),NameUser(B)),AccessExp(NameExp(NameUser(@@RG)),NameUser(A))),LiteralExp(INTEGER,7,7ii))
                    BinaryExp(-,AccessExp(NameExp(NameUser(@@RG)),NameUser(B)),AccessExp(NameExp(NameUser(@@RG)),NameUser(A)))
                      AccessExp(NameExp(NameUser(@@RG)),NameUser(B))
                        NameExp(NameUser(@@RG))
                          NameUser(@@RG)
                        NameUser(B)
                      AccessExp(NameExp(NameUser(@@RG)),NameUser(A))
                        NameExp(NameUser(@@RG))
                          NameUser(@@RG)
                        NameUser(A)
                    LiteralExp(INTEGER,7,7ii)
              JumpLocation(Some(NameDefinition(l6)),Vector(),ReturnJump(Vector(),None))
                Some(NameDefinition(l6))
                  NameDefinition(l6)
                List()
                ReturnJump(Vector(),None)
                  List()
                  None
            List()
Model(Some(verysimplerecord.ads),Vector(),Vector(PackageDecl(Some(NameDefinition(SimpleRecord)),Vector(),List(ProcedureDecl(NameDefinition(Dummy),Vect
  Some(verysimplerecord.ads)
  List()
  Vector(PackageDecl(Some(NameDefinition(SimpleRecord)),Vector(),List(ProcedureDecl(NameDefinition(Dummy),Vector(),Vector(),List(),Some(NamedTypeSpec(Na
    PackageDecl(Some(NameDefinition(SimpleRecord)),Vector(),List(ProcedureDecl(NameDefinition(Dummy),Vector(),Vector(),List(),Some(NamedTypeSpec(NameUser(
      Some(NameDefinition(SimpleRecord))
        NameDefinition(SimpleRecord)
      List()
      List(ProcedureDecl(NameDefinition(Dummy),Vector(),Vector(),List(),Some(NamedTypeSpec(NameUser(Boolean),Vector())),false,EmptyBody()))
        ProcedureDecl(NameDefinition(Dummy),Vector(),Vector(),List(),Some(NamedTypeSpec(NameUser(Boolean),Vector())),false,EmptyBody())
          NameDefinition(Dummy)
          List()
          List()
          List()
          Some(NamedTypeSpec(NameUser(Boolean),Vector()))
            NamedTypeSpec(NameUser(Boolean),Vector())
              NameUser(Boolean)
              List()
          EmptyBody()
Model(Some(fake/standard.ads),Vector(),Vector(PackageDecl(Some(NameDefinition(Standard)),Vector(),List(TypeAliasDecl(NameDefinition(standard::boolean)
  Some(fake/standard.ads)
  List()
  Vector(PackageDecl(Some(NameDefinition(Standard)),Vector(),List(TypeAliasDecl(NameDefinition(standard::boolean),Vector(),NamedTypeSpec(NameUser(Boolea
    PackageDecl(Some(NameDefinition(Standard)),Vector(),List(TypeAliasDecl(NameDefinition(standard::boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List
      Some(NameDefinition(Standard))
        NameDefinition(Standard)
      List()
      List(TypeAliasDecl(NameDefinition(standard::boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List())), TypeAliasDecl(NameDefinition(standard::integer
        TypeAliasDecl(NameDefinition(standard::boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List()))
          NameDefinition(standard::boolean)
          List()
          NamedTypeSpec(NameUser(Boolean),List())
            NameUser(Boolean)
            List()
        TypeAliasDecl(NameDefinition(standard::integer),Vector(),NamedTypeSpec(NameUser(Integer),List()))
          NameDefinition(standard::integer)
          List()
          NamedTypeSpec(NameUser(Integer),List())
            NameUser(Integer)
            List()
        TypeAliasDecl(NameDefinition(standard::natural),Vector(),NamedTypeSpec(NameUser(Integer),List()))
          NameDefinition(standard::natural)
          List()
          NamedTypeSpec(NameUser(Integer),List())
            NameUser(Integer)
            List()
        TypeAliasDecl(NameDefinition(standard::positive),Vector(),NamedTypeSpec(NameUser(Integer),List()))
          NameDefinition(standard::positive)
          List()
          NamedTypeSpec(NameUser(Integer),List())
            NameUser(Integer)
            List()
        TypeAliasDecl(NameDefinition(standard::float),Vector(),NamedTypeSpec(NameUser(Float),List()))
          NameDefinition(standard::float)
          List()
          NamedTypeSpec(NameUser(Float),List())
            NameUser(Float)
            List()
        TypeAliasDecl(NameDefinition(universal_integer),Vector(),NamedTypeSpec(NameUser(Integer),List()))
          NameDefinition(universal_integer)
          List()
          NamedTypeSpec(NameUser(Integer),List())
            NameUser(Integer)
            List()
        TypeAliasDecl(NameDefinition(universal_real),Vector(),NamedTypeSpec(NameUser(Float),List()))
          NameDefinition(universal_real)
          List()
          NamedTypeSpec(NameUser(Float),List())
            NameUser(Float)
            List()

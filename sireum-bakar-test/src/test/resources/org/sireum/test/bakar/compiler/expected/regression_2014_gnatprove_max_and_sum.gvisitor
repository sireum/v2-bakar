Model(Some(maxandsum.adb),Vector(),Vector(PackageDecl(Some(NameDefinition(MaxAndSum)),Vector(),List(ProcedureDecl(NameDefinition(MaxAndSum),Vector(),V
  Some(maxandsum.adb)
  List()
  Vector(PackageDecl(Some(NameDefinition(MaxAndSum)),Vector(),List(ProcedureDecl(NameDefinition(MaxAndSum),Vector(),Vector(),List(ParamDecl(Some(NamedTy
    PackageDecl(Some(NameDefinition(MaxAndSum)),Vector(),List(ProcedureDecl(NameDefinition(MaxAndSum),Vector(),Vector(),List(ParamDecl(Some(NamedTypeSpec(
      Some(NameDefinition(MaxAndSum))
        NameDefinition(MaxAndSum)
      List()
      List(ProcedureDecl(NameDefinition(MaxAndSum),Vector(),Vector(),List(ParamDecl(Some(NamedTypeSpec(NameUser(ElementArray),Vector())),NameDefinition(A),V
        ProcedureDecl(NameDefinition(MaxAndSum),Vector(),Vector(),List(ParamDecl(Some(NamedTypeSpec(NameUser(ElementArray),Vector())),NameDefinition(A),Vector
          NameDefinition(MaxAndSum)
          List()
          List()
          List(ParamDecl(Some(NamedTypeSpec(NameUser(ElementArray),Vector())),NameDefinition(A),Vector()), ParamDecl(Some(NamedTypeSpec(NameUser(Natural),Vector
            ParamDecl(Some(NamedTypeSpec(NameUser(ElementArray),Vector())),NameDefinition(A),Vector())
              Some(NamedTypeSpec(NameUser(ElementArray),Vector()))
                NamedTypeSpec(NameUser(ElementArray),Vector())
                  NameUser(ElementArray)
                  List()
              NameDefinition(A)
              List()
            ParamDecl(Some(NamedTypeSpec(NameUser(Natural),Vector())),NameDefinition(Sum),Vector())
              Some(NamedTypeSpec(NameUser(Natural),Vector()))
                NamedTypeSpec(NameUser(Natural),Vector())
                  NameUser(Natural)
                  List()
              NameDefinition(Sum)
              List()
            ParamDecl(Some(NamedTypeSpec(NameUser(Element),Vector())),NameDefinition(Max),Vector())
              Some(NamedTypeSpec(NameUser(Element),Vector()))
                NamedTypeSpec(NameUser(Element),Vector())
                  NameUser(Element)
                  List()
              NameDefinition(Max)
              List()
          None
          ImplementedBody(List(LocalVarDecl(Some(NamedTypeSpec(NameUser(universal_integer),Vector())),NameDefinition(I),Vector()), LocalVarDecl(Some(NamedTypeSp
            List(LocalVarDecl(Some(NamedTypeSpec(NameUser(universal_integer),Vector())),NameDefinition(I),Vector()), LocalVarDecl(Some(NamedTypeSpec(NameUser(univ
              LocalVarDecl(Some(NamedTypeSpec(NameUser(universal_integer),Vector())),NameDefinition(I),Vector())
                Some(NamedTypeSpec(NameUser(universal_integer),Vector()))
                  NamedTypeSpec(NameUser(universal_integer),Vector())
                    NameUser(universal_integer)
                    List()
                NameDefinition(I)
                List()
              LocalVarDecl(Some(NamedTypeSpec(NameUser(universal_integer),List())),NameDefinition(_tcomp0),List())
                Some(NamedTypeSpec(NameUser(universal_integer),List()))
                  NamedTypeSpec(NameUser(universal_integer),List())
                    NameUser(universal_integer)
                    List()
                NameDefinition(_tcomp0)
                List()
              LocalVarDecl(Some(NamedTypeSpec(NameUser(universal_integer),List())),NameDefinition(_tcomp1),List())
                Some(NamedTypeSpec(NameUser(universal_integer),List()))
                  NamedTypeSpec(NameUser(universal_integer),List())
                    NameUser(universal_integer)
                    List()
                NameDefinition(_tcomp1)
                List()
            List(ActionLocation(Some(NameDefinition(l1)),Vector(),AssignAction(Vector(),NameExp(NameUser(Sum)),:=,LiteralExp(INTEGER,0,0ii))), ActionLocation(Some
              ActionLocation(Some(NameDefinition(l1)),Vector(),AssignAction(Vector(),NameExp(NameUser(Sum)),:=,LiteralExp(INTEGER,0,0ii)))
                Some(NameDefinition(l1))
                  NameDefinition(l1)
                List()
                AssignAction(Vector(),NameExp(NameUser(Sum)),:=,LiteralExp(INTEGER,0,0ii))
                  List()
                  NameExp(NameUser(Sum))
                    NameUser(Sum)
                  LiteralExp(INTEGER,0,0ii)
              ActionLocation(Some(NameDefinition(l2)),Vector(),AssignAction(Vector(),NameExp(NameUser(Max)),:=,LiteralExp(INTEGER,0,0ii)))
                Some(NameDefinition(l2))
                  NameDefinition(l2)
                List()
                AssignAction(Vector(),NameExp(NameUser(Max)),:=,LiteralExp(INTEGER,0,0ii))
                  List()
                  NameExp(NameUser(Max))
                    NameUser(Max)
                  LiteralExp(INTEGER,0,0ii)
              ActionLocation(Some(NameDefinition(l4)),Vector(),AssignAction(Vector(),NameExp(NameUser(_tcomp0)),:=,LiteralExp(INTEGER,1,1ii)))
                Some(NameDefinition(l4))
                  NameDefinition(l4)
                List()
                AssignAction(Vector(),NameExp(NameUser(_tcomp0)),:=,LiteralExp(INTEGER,1,1ii))
                  List()
                  NameExp(NameUser(_tcomp0))
                    NameUser(_tcomp0)
                  LiteralExp(INTEGER,1,1ii)
              ActionLocation(Some(NameDefinition(l5)),Vector(),AssignAction(Vector(),NameExp(NameUser(_tcomp1)),:=,NameExp(NameUser(N))))
                Some(NameDefinition(l5))
                  NameDefinition(l5)
                List()
                AssignAction(Vector(),NameExp(NameUser(_tcomp1)),:=,NameExp(NameUser(N)))
                  List()
                  NameExp(NameUser(_tcomp1))
                    NameUser(_tcomp1)
                  NameExp(NameUser(N))
                    NameUser(N)
              JumpLocation(Some(NameDefinition(l6)),Vector(),IfJump(Vector(),Vector(IfThenJump(BinaryExp(>,NameExp(NameUser(_tcomp0)),NameExp(NameUser(_tcomp1))),Ve
                Some(NameDefinition(l6))
                  NameDefinition(l6)
                List()
                IfJump(Vector(),Vector(IfThenJump(BinaryExp(>,NameExp(NameUser(_tcomp0)),NameExp(NameUser(_tcomp1))),Vector(),NameUser(l3))),None)
                  List()
                  Vector(IfThenJump(BinaryExp(>,NameExp(NameUser(_tcomp0)),NameExp(NameUser(_tcomp1))),Vector(),NameUser(l3)))
                    IfThenJump(BinaryExp(>,NameExp(NameUser(_tcomp0)),NameExp(NameUser(_tcomp1))),Vector(),NameUser(l3))
                      BinaryExp(>,NameExp(NameUser(_tcomp0)),NameExp(NameUser(_tcomp1)))
                        NameExp(NameUser(_tcomp0))
                          NameUser(_tcomp0)
                        NameExp(NameUser(_tcomp1))
                          NameUser(_tcomp1)
                      List()
                      NameUser(l3)
                  None
              ActionLocation(Some(NameDefinition(l7)),Vector(),AssignAction(Vector(),NameExp(NameUser(I)),:=,NameExp(NameUser(_tcomp0))))
                Some(NameDefinition(l7))
                  NameDefinition(l7)
                List()
                AssignAction(Vector(),NameExp(NameUser(I)),:=,NameExp(NameUser(_tcomp0)))
                  List()
                  NameExp(NameUser(I))
                    NameUser(I)
                  NameExp(NameUser(_tcomp0))
                    NameUser(_tcomp0)
              EmptyLocation(Some(NameDefinition(l8)),Vector())
                Some(NameDefinition(l8))
                  NameDefinition(l8)
                List()
              ActionLocation(Some(NameDefinition(l9)),Vector(),AssertAction(Vector(),CallExp(NameExp(NameUser(proof__uif__loop_invariant)),BinaryExp(<=,NameExp(Name
                Some(NameDefinition(l9))
                  NameDefinition(l9)
                List()
                AssertAction(Vector(),CallExp(NameExp(NameUser(proof__uif__loop_invariant)),BinaryExp(<=,NameExp(NameUser(Sum)),BinaryExp(*,BinaryExp(-,NameExp(NameUs
                  List()
                  CallExp(NameExp(NameUser(proof__uif__loop_invariant)),BinaryExp(<=,NameExp(NameUser(Sum)),BinaryExp(*,BinaryExp(-,NameExp(NameUser(I)),LiteralExp(INTE
                    NameExp(NameUser(proof__uif__loop_invariant))
                      NameUser(proof__uif__loop_invariant)
                    BinaryExp(<=,NameExp(NameUser(Sum)),BinaryExp(*,BinaryExp(-,NameExp(NameUser(I)),LiteralExp(INTEGER,1,1ii)),NameExp(NameUser(Max))))
                      NameExp(NameUser(Sum))
                        NameUser(Sum)
                      BinaryExp(*,BinaryExp(-,NameExp(NameUser(I)),LiteralExp(INTEGER,1,1ii)),NameExp(NameUser(Max)))
                        BinaryExp(-,NameExp(NameUser(I)),LiteralExp(INTEGER,1,1ii))
                          NameExp(NameUser(I))
                            NameUser(I)
                          LiteralExp(INTEGER,1,1ii)
                        NameExp(NameUser(Max))
                          NameUser(Max)
                  None
              ActionLocation(Some(NameDefinition(l10)),Vector(),AssignAction(Vector(),NameExp(NameUser(Sum)),:=,BinaryExp(+,NameExp(NameUser(Sum)),IndexingExp(NameE
                Some(NameDefinition(l10))
                  NameDefinition(l10)
                List()
                AssignAction(Vector(),NameExp(NameUser(Sum)),:=,BinaryExp(+,NameExp(NameUser(Sum)),IndexingExp(NameExp(NameUser(A)),List(NameExp(NameUser(I))))))
                  List()
                  NameExp(NameUser(Sum))
                    NameUser(Sum)
                  BinaryExp(+,NameExp(NameUser(Sum)),IndexingExp(NameExp(NameUser(A)),List(NameExp(NameUser(I)))))
                    NameExp(NameUser(Sum))
                      NameUser(Sum)
                    IndexingExp(NameExp(NameUser(A)),List(NameExp(NameUser(I))))
                      NameExp(NameUser(A))
                        NameUser(A)
                      List(NameExp(NameUser(I)))
                        NameExp(NameUser(I))
                          NameUser(I)
              JumpLocation(Some(NameDefinition(l12)),Vector(),IfJump(Vector(),Vector(IfThenJump(UnaryExp(!,BinaryExp(<,NameExp(NameUser(Max)),IndexingExp(NameExp(Na
                Some(NameDefinition(l12))
                  NameDefinition(l12)
                List()
                IfJump(Vector(),Vector(IfThenJump(UnaryExp(!,BinaryExp(<,NameExp(NameUser(Max)),IndexingExp(NameExp(NameUser(A)),List(NameExp(NameUser(I)))))),Vector(
                  List()
                  Vector(IfThenJump(UnaryExp(!,BinaryExp(<,NameExp(NameUser(Max)),IndexingExp(NameExp(NameUser(A)),List(NameExp(NameUser(I)))))),Vector(),NameUser(l11))
                    IfThenJump(UnaryExp(!,BinaryExp(<,NameExp(NameUser(Max)),IndexingExp(NameExp(NameUser(A)),List(NameExp(NameUser(I)))))),Vector(),NameUser(l11))
                      UnaryExp(!,BinaryExp(<,NameExp(NameUser(Max)),IndexingExp(NameExp(NameUser(A)),List(NameExp(NameUser(I))))))
                        BinaryExp(<,NameExp(NameUser(Max)),IndexingExp(NameExp(NameUser(A)),List(NameExp(NameUser(I)))))
                          NameExp(NameUser(Max))
                            NameUser(Max)
                          IndexingExp(NameExp(NameUser(A)),List(NameExp(NameUser(I))))
                            NameExp(NameUser(A))
                              NameUser(A)
                            List(NameExp(NameUser(I)))
                              NameExp(NameUser(I))
                                NameUser(I)
                      List()
                      NameUser(l11)
                  None
              ActionLocation(Some(NameDefinition(l13)),Vector(),AssignAction(Vector(),NameExp(NameUser(Max)),:=,IndexingExp(NameExp(NameUser(A)),List(NameExp(NameUs
                Some(NameDefinition(l13))
                  NameDefinition(l13)
                List()
                AssignAction(Vector(),NameExp(NameUser(Max)),:=,IndexingExp(NameExp(NameUser(A)),List(NameExp(NameUser(I)))))
                  List()
                  NameExp(NameUser(Max))
                    NameUser(Max)
                  IndexingExp(NameExp(NameUser(A)),List(NameExp(NameUser(I))))
                    NameExp(NameUser(A))
                      NameUser(A)
                    List(NameExp(NameUser(I)))
                      NameExp(NameUser(I))
                        NameUser(I)
              EmptyLocation(Some(NameDefinition(l11)),Vector())
                Some(NameDefinition(l11))
                  NameDefinition(l11)
                List()
              ActionLocation(Some(NameDefinition(l14)),Vector(),AssertAction(Vector(),BinaryExp(<=,NameExp(NameUser(Sum)),BinaryExp(*,NameExp(NameUser(I)),NameExp(N
                Some(NameDefinition(l14))
                  NameDefinition(l14)
                List()
                AssertAction(Vector(),BinaryExp(<=,NameExp(NameUser(Sum)),BinaryExp(*,NameExp(NameUser(I)),NameExp(NameUser(Max)))),None)
                  List()
                  BinaryExp(<=,NameExp(NameUser(Sum)),BinaryExp(*,NameExp(NameUser(I)),NameExp(NameUser(Max))))
                    NameExp(NameUser(Sum))
                      NameUser(Sum)
                    BinaryExp(*,NameExp(NameUser(I)),NameExp(NameUser(Max)))
                      NameExp(NameUser(I))
                        NameUser(I)
                      NameExp(NameUser(Max))
                        NameUser(Max)
                  None
              JumpLocation(Some(NameDefinition(l15)),Vector(),IfJump(Vector(),Vector(IfThenJump(BinaryExp(==,NameExp(NameUser(I)),NameExp(NameUser(_tcomp1))),Vector
                Some(NameDefinition(l15))
                  NameDefinition(l15)
                List()
                IfJump(Vector(),Vector(IfThenJump(BinaryExp(==,NameExp(NameUser(I)),NameExp(NameUser(_tcomp1))),Vector(),NameUser(l3))),None)
                  List()
                  Vector(IfThenJump(BinaryExp(==,NameExp(NameUser(I)),NameExp(NameUser(_tcomp1))),Vector(),NameUser(l3)))
                    IfThenJump(BinaryExp(==,NameExp(NameUser(I)),NameExp(NameUser(_tcomp1))),Vector(),NameUser(l3))
                      BinaryExp(==,NameExp(NameUser(I)),NameExp(NameUser(_tcomp1)))
                        NameExp(NameUser(I))
                          NameUser(I)
                        NameExp(NameUser(_tcomp1))
                          NameUser(_tcomp1)
                      List()
                      NameUser(l3)
                  None
              ActionLocation(Some(NameDefinition(l16)),Vector(),AssignAction(Vector(),NameExp(NameUser(I)),:=,BinaryExp(+,NameExp(NameUser(I)),LiteralExp(INTEGER,1,
                Some(NameDefinition(l16))
                  NameDefinition(l16)
                List()
                AssignAction(Vector(),NameExp(NameUser(I)),:=,BinaryExp(+,NameExp(NameUser(I)),LiteralExp(INTEGER,1,1ii)))
                  List()
                  NameExp(NameUser(I))
                    NameUser(I)
                  BinaryExp(+,NameExp(NameUser(I)),LiteralExp(INTEGER,1,1ii))
                    NameExp(NameUser(I))
                      NameUser(I)
                    LiteralExp(INTEGER,1,1ii)
              JumpLocation(Some(NameDefinition(l17)),Vector(),GotoJump(Vector(),NameUser(l8)))
                Some(NameDefinition(l17))
                  NameDefinition(l17)
                List()
                GotoJump(Vector(),NameUser(l8))
                  List()
                  NameUser(l8)
              EmptyLocation(Some(NameDefinition(l3)),Vector())
                Some(NameDefinition(l3))
                  NameDefinition(l3)
                List()
              JumpLocation(Some(NameDefinition(l18)),Vector(),ReturnJump(Vector(),None))
                Some(NameDefinition(l18))
                  NameDefinition(l18)
                List()
                ReturnJump(Vector(),None)
                  List()
                  None
            List()
Model(Some(maxandsum.ads),Vector(),Vector(PackageDecl(Some(NameDefinition(MaxAndSum)),Vector(),List(TypeAliasDecl(NameDefinition(Element),Vector(),Nam
  Some(maxandsum.ads)
  List()
  Vector(PackageDecl(Some(NameDefinition(MaxAndSum)),Vector(),List(TypeAliasDecl(NameDefinition(Element),Vector(),NamedTypeSpec(NameUser(Natural),List()
    PackageDecl(Some(NameDefinition(MaxAndSum)),Vector(),List(TypeAliasDecl(NameDefinition(Element),Vector(),NamedTypeSpec(NameUser(Natural),List())), Typ
      Some(NameDefinition(MaxAndSum))
        NameDefinition(MaxAndSum)
      List()
      List(TypeAliasDecl(NameDefinition(Element),Vector(),NamedTypeSpec(NameUser(Natural),List())), TypeAliasDecl(NameDefinition(anonymousType$0),Vector(),N
        TypeAliasDecl(NameDefinition(Element),Vector(),NamedTypeSpec(NameUser(Natural),List()))
          NameDefinition(Element)
          List()
          NamedTypeSpec(NameUser(Natural),List())
            NameUser(Natural)
            List()
        TypeAliasDecl(NameDefinition(anonymousType$0),Vector(),NamedTypeSpec(NameUser(Positive),List()))
          NameDefinition(anonymousType$0)
          List()
          NamedTypeSpec(NameUser(Positive),List())
            NameUser(Positive)
            List()
        TypeAliasDecl(NameDefinition(ElementArray),Vector(),NamedTypeSpec(NameUser(_ARRAY_),List()))
          NameDefinition(ElementArray)
          List()
          NamedTypeSpec(NameUser(_ARRAY_),List())
            NameUser(_ARRAY_)
            List()
        ConstDecl(NameDefinition($CONST),Vector(),List(ConstElement(NameDefinition(N),CastExp(NamedTypeSpec(NameUser(Natural),Vector()),LiteralExp(INTEGER,100
          NameDefinition($CONST)
          List()
          List(ConstElement(NameDefinition(N),CastExp(NamedTypeSpec(NameUser(Natural),Vector()),LiteralExp(INTEGER,100,100ii)),Vector()))
            ConstElement(NameDefinition(N),CastExp(NamedTypeSpec(NameUser(Natural),Vector()),LiteralExp(INTEGER,100,100ii)),Vector())
              NameDefinition(N)
              CastExp(NamedTypeSpec(NameUser(Natural),Vector()),LiteralExp(INTEGER,100,100ii))
                NamedTypeSpec(NameUser(Natural),Vector())
                  NameUser(Natural)
                  List()
                LiteralExp(INTEGER,100,100ii)
              List()
        ProcedureDecl(NameDefinition(MaxAndSum),Vector(),Vector(),List(ParamDecl(Some(NamedTypeSpec(NameUser(ElementArray),Vector())),NameDefinition(A),Vector
          NameDefinition(MaxAndSum)
          List()
          List()
          List(ParamDecl(Some(NamedTypeSpec(NameUser(ElementArray),Vector())),NameDefinition(A),Vector()), ParamDecl(Some(NamedTypeSpec(NameUser(Natural),Vector
            ParamDecl(Some(NamedTypeSpec(NameUser(ElementArray),Vector())),NameDefinition(A),Vector())
              Some(NamedTypeSpec(NameUser(ElementArray),Vector()))
                NamedTypeSpec(NameUser(ElementArray),Vector())
                  NameUser(ElementArray)
                  List()
              NameDefinition(A)
              List()
            ParamDecl(Some(NamedTypeSpec(NameUser(Natural),Vector())),NameDefinition(Sum),Vector())
              Some(NamedTypeSpec(NameUser(Natural),Vector()))
                NamedTypeSpec(NameUser(Natural),Vector())
                  NameUser(Natural)
                  List()
              NameDefinition(Sum)
              List()
            ParamDecl(Some(NamedTypeSpec(NameUser(Element),Vector())),NameDefinition(Max),Vector())
              Some(NamedTypeSpec(NameUser(Element),Vector()))
                NamedTypeSpec(NameUser(Element),Vector())
                  NameUser(Element)
                  List()
              NameDefinition(Max)
              List()
          None
          EmptyBody()
Model(Some(fake/standard.ads),Vector(),Vector(PackageDecl(Some(NameDefinition(Standard)),Vector(),List(TypeAliasDecl(NameDefinition(Boolean),Vector(),
  Some(fake/standard.ads)
  List()
  Vector(PackageDecl(Some(NameDefinition(Standard)),Vector(),List(TypeAliasDecl(NameDefinition(Boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List())
    PackageDecl(Some(NameDefinition(Standard)),Vector(),List(TypeAliasDecl(NameDefinition(Boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List())), Type
      Some(NameDefinition(Standard))
        NameDefinition(Standard)
      List()
      List(TypeAliasDecl(NameDefinition(Boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List())), TypeAliasDecl(NameDefinition(Integer),Vector(),NamedType
        TypeAliasDecl(NameDefinition(Boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List()))
          NameDefinition(Boolean)
          List()
          NamedTypeSpec(NameUser(Boolean),List())
            NameUser(Boolean)
            List()
        TypeAliasDecl(NameDefinition(Integer),Vector(),NamedTypeSpec(NameUser(Integer),List()))
          NameDefinition(Integer)
          List()
          NamedTypeSpec(NameUser(Integer),List())
            NameUser(Integer)
            List()
        TypeAliasDecl(NameDefinition(Natural),Vector(),NamedTypeSpec(NameUser(Integer),List()))
          NameDefinition(Natural)
          List()
          NamedTypeSpec(NameUser(Integer),List())
            NameUser(Integer)
            List()
        TypeAliasDecl(NameDefinition(Positive),Vector(),NamedTypeSpec(NameUser(Integer),List()))
          NameDefinition(Positive)
          List()
          NamedTypeSpec(NameUser(Integer),List())
            NameUser(Integer)
            List()
        TypeAliasDecl(NameDefinition(Float),Vector(),NamedTypeSpec(NameUser(Float),List()))
          NameDefinition(Float)
          List()
          NamedTypeSpec(NameUser(Float),List())
            NameUser(Float)
            List()
        TypeAliasDecl(NameDefinition(universal_integer),Vector(),NamedTypeSpec(NameUser(universal_integer),List()))
          NameDefinition(universal_integer)
          List()
          NamedTypeSpec(NameUser(universal_integer),List())
            NameUser(universal_integer)
            List()
        TypeAliasDecl(NameDefinition(universal_real),Vector(),NamedTypeSpec(NameUser(universal_real),List()))
          NameDefinition(universal_real)
          List()
          NamedTypeSpec(NameUser(universal_real),List())
            NameUser(universal_real)
            List()
        TypeAliasDecl(NameDefinition(Character),Vector(),NamedTypeSpec(NameUser(Character),List()))
          NameDefinition(Character)
          List()
          NamedTypeSpec(NameUser(Character),List())
            NameUser(Character)
            List()
        TypeAliasDecl(NameDefinition(String),Vector(),NamedTypeSpec(NameUser(String),List()))
          NameDefinition(String)
          List()
          NamedTypeSpec(NameUser(String),List())
            NameUser(String)
            List()

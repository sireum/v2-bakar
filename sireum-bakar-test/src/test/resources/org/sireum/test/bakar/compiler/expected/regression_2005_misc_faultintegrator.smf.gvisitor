Model(Some(faultintegrator.adb),Vector(),Vector(PackageDecl(Some(NameDefinition(FaultIntegrator)),Vector(),List(ProcedureDecl(NameDefinition(Init),Vec
  Some(faultintegrator.adb)
  List()
  Vector(PackageDecl(Some(NameDefinition(FaultIntegrator)),Vector(),List(ProcedureDecl(NameDefinition(Init),Vector(),Vector(),List(ParamDecl(Some(NamedT
    PackageDecl(Some(NameDefinition(FaultIntegrator)),Vector(),List(ProcedureDecl(NameDefinition(Init),Vector(),Vector(),List(ParamDecl(Some(NamedTypeSpec
      Some(NameDefinition(FaultIntegrator))
        NameDefinition(FaultIntegrator)
      List()
      List(ProcedureDecl(NameDefinition(Init),Vector(),Vector(),List(ParamDecl(Some(NamedTypeSpec(NameUser(T),Vector())),NameDefinition(FI),Vector()), Param
        ProcedureDecl(NameDefinition(Init),Vector(),Vector(),List(ParamDecl(Some(NamedTypeSpec(NameUser(T),Vector())),NameDefinition(FI),Vector()), ParamDecl(
          NameDefinition(Init)
          List()
          List()
          List(ParamDecl(Some(NamedTypeSpec(NameUser(T),Vector())),NameDefinition(FI),Vector()), ParamDecl(Some(NamedTypeSpec(NameUser(Positive),Vector())),Name
            ParamDecl(Some(NamedTypeSpec(NameUser(T),Vector())),NameDefinition(FI),Vector())
              Some(NamedTypeSpec(NameUser(T),Vector()))
                NamedTypeSpec(NameUser(T),Vector())
                  NameUser(T)
                  List()
              NameDefinition(FI)
              List()
            ParamDecl(Some(NamedTypeSpec(NameUser(Positive),Vector())),NameDefinition(Threshold),Vector())
              Some(NamedTypeSpec(NameUser(Positive),Vector()))
                NamedTypeSpec(NameUser(Positive),Vector())
                  NameUser(Positive)
                  List()
              NameDefinition(Threshold)
              List()
          None
          ImplementedBody(List(),List(ActionLocation(Some(NameDefinition(l1)),Vector(),AssignAction(Vector(),NameExp(NameUser(FI)),:=,NewRecordExp(NamedTypeSpec
            List()
            List(ActionLocation(Some(NameDefinition(l1)),Vector(),AssignAction(Vector(),NameExp(NameUser(FI)),:=,NewRecordExp(NamedTypeSpec(NameUser(T),Vector()),
              ActionLocation(Some(NameDefinition(l1)),Vector(),AssignAction(Vector(),NameExp(NameUser(FI)),:=,NewRecordExp(NamedTypeSpec(NameUser(T),Vector()),Vecto
                Some(NameDefinition(l1))
                  NameDefinition(l1)
                List()
                AssignAction(Vector(),NameExp(NameUser(FI)),:=,NewRecordExp(NamedTypeSpec(NameUser(T),Vector()),Vector(AttributeInit(NameUser(Limit),NameExp(NameUser(
                  List()
                  NameExp(NameUser(FI))
                    NameUser(FI)
                  NewRecordExp(NamedTypeSpec(NameUser(T),Vector()),Vector(AttributeInit(NameUser(Limit),NameExp(NameUser(Threshold))), AttributeInit(NameUser(Counter),L
                    NamedTypeSpec(NameUser(T),Vector())
                      NameUser(T)
                      List()
                    Vector(AttributeInit(NameUser(Limit),NameExp(NameUser(Threshold))), AttributeInit(NameUser(Counter),LiteralExp(INTEGER,0,0ii)), AttributeInit(NameUser
                      AttributeInit(NameUser(Limit),NameExp(NameUser(Threshold)))
                        NameUser(Limit)
                        NameExp(NameUser(Threshold))
                          NameUser(Threshold)
                      AttributeInit(NameUser(Counter),LiteralExp(INTEGER,0,0ii))
                        NameUser(Counter)
                        LiteralExp(INTEGER,0,0ii)
                      AttributeInit(NameUser(Tripped),LiteralExp(BOOLEAN,false,false))
                        NameUser(Tripped)
                        LiteralExp(BOOLEAN,false,false)
              JumpLocation(Some(NameDefinition(l2)),Vector(),ReturnJump(Vector(),None))
                Some(NameDefinition(l2))
                  NameDefinition(l2)
                List()
                ReturnJump(Vector(),None)
                  List()
                  None
            List()
        ProcedureDecl(NameDefinition(Test),Vector(),Vector(),List(ParamDecl(Some(NamedTypeSpec(NameUser(T),Vector())),NameDefinition(FI),Vector()), ParamDecl(
          NameDefinition(Test)
          List()
          List()
          List(ParamDecl(Some(NamedTypeSpec(NameUser(T),Vector())),NameDefinition(FI),Vector()), ParamDecl(Some(NamedTypeSpec(NameUser(Boolean),Vector())),NameD
            ParamDecl(Some(NamedTypeSpec(NameUser(T),Vector())),NameDefinition(FI),Vector())
              Some(NamedTypeSpec(NameUser(T),Vector()))
                NamedTypeSpec(NameUser(T),Vector())
                  NameUser(T)
                  List()
              NameDefinition(FI)
              List()
            ParamDecl(Some(NamedTypeSpec(NameUser(Boolean),Vector())),NameDefinition(CurrentEvent),Vector())
              Some(NamedTypeSpec(NameUser(Boolean),Vector()))
                NamedTypeSpec(NameUser(Boolean),Vector())
                  NameUser(Boolean)
                  List()
              NameDefinition(CurrentEvent)
              List()
            ParamDecl(Some(NamedTypeSpec(NameUser(Boolean),Vector())),NameDefinition(IntegratedEvent),Vector())
              Some(NamedTypeSpec(NameUser(Boolean),Vector()))
                NamedTypeSpec(NameUser(Boolean),Vector())
                  NameUser(Boolean)
                  List()
              NameDefinition(IntegratedEvent)
              List()
          None
          ImplementedBody(List(),List(JumpLocation(Some(NameDefinition(l5)),Vector(),IfJump(Vector(),Vector(IfThenJump(UnaryExp(!,NameExp(NameUser(CurrentEvent)
            List()
            List(JumpLocation(Some(NameDefinition(l5)),Vector(),IfJump(Vector(),Vector(IfThenJump(UnaryExp(!,NameExp(NameUser(CurrentEvent))),Vector(),NameUser(l4
              JumpLocation(Some(NameDefinition(l5)),Vector(),IfJump(Vector(),Vector(IfThenJump(UnaryExp(!,NameExp(NameUser(CurrentEvent))),Vector(),NameUser(l4))),N
                Some(NameDefinition(l5))
                  NameDefinition(l5)
                List()
                IfJump(Vector(),Vector(IfThenJump(UnaryExp(!,NameExp(NameUser(CurrentEvent))),Vector(),NameUser(l4))),None)
                  List()
                  Vector(IfThenJump(UnaryExp(!,NameExp(NameUser(CurrentEvent))),Vector(),NameUser(l4)))
                    IfThenJump(UnaryExp(!,NameExp(NameUser(CurrentEvent))),Vector(),NameUser(l4))
                      UnaryExp(!,NameExp(NameUser(CurrentEvent)))
                        NameExp(NameUser(CurrentEvent))
                          NameUser(CurrentEvent)
                      List()
                      NameUser(l4)
                  None
              JumpLocation(Some(NameDefinition(l8)),Vector(),IfJump(Vector(),Vector(IfThenJump(UnaryExp(!,BinaryExp(==,AccessExp(NameExp(NameUser(FI)),NameUser(Coun
                Some(NameDefinition(l8))
                  NameDefinition(l8)
                List()
                IfJump(Vector(),Vector(IfThenJump(UnaryExp(!,BinaryExp(==,AccessExp(NameExp(NameUser(FI)),NameUser(Counter)),AccessExp(NameExp(NameUser(FI)),NameUser(
                  List()
                  Vector(IfThenJump(UnaryExp(!,BinaryExp(==,AccessExp(NameExp(NameUser(FI)),NameUser(Counter)),AccessExp(NameExp(NameUser(FI)),NameUser(Limit)))),Vector
                    IfThenJump(UnaryExp(!,BinaryExp(==,AccessExp(NameExp(NameUser(FI)),NameUser(Counter)),AccessExp(NameExp(NameUser(FI)),NameUser(Limit)))),Vector(),Name
                      UnaryExp(!,BinaryExp(==,AccessExp(NameExp(NameUser(FI)),NameUser(Counter)),AccessExp(NameExp(NameUser(FI)),NameUser(Limit))))
                        BinaryExp(==,AccessExp(NameExp(NameUser(FI)),NameUser(Counter)),AccessExp(NameExp(NameUser(FI)),NameUser(Limit)))
                          AccessExp(NameExp(NameUser(FI)),NameUser(Counter))
                            NameExp(NameUser(FI))
                              NameUser(FI)
                            NameUser(Counter)
                          AccessExp(NameExp(NameUser(FI)),NameUser(Limit))
                            NameExp(NameUser(FI))
                              NameUser(FI)
                            NameUser(Limit)
                      List()
                      NameUser(l7)
                  None
              ActionLocation(Some(NameDefinition(l9)),Vector(),AssignAction(Vector(),AccessExp(NameExp(NameUser(FI)),NameUser(Tripped)),:=,LiteralExp(BOOLEAN,true,t
                Some(NameDefinition(l9))
                  NameDefinition(l9)
                List()
                AssignAction(Vector(),AccessExp(NameExp(NameUser(FI)),NameUser(Tripped)),:=,LiteralExp(BOOLEAN,true,true))
                  List()
                  AccessExp(NameExp(NameUser(FI)),NameUser(Tripped))
                    NameExp(NameUser(FI))
                      NameUser(FI)
                    NameUser(Tripped)
                  LiteralExp(BOOLEAN,true,true)
              JumpLocation(Some(NameDefinition(l10)),Vector(),GotoJump(Vector(),NameUser(l6)))
                Some(NameDefinition(l10))
                  NameDefinition(l10)
                List()
                GotoJump(Vector(),NameUser(l6))
                  List()
                  NameUser(l6)
              EmptyLocation(Some(NameDefinition(l7)),Vector())
                Some(NameDefinition(l7))
                  NameDefinition(l7)
                List()
              ActionLocation(Some(NameDefinition(l11)),Vector(),AssignAction(Vector(),AccessExp(NameExp(NameUser(FI)),NameUser(Counter)),:=,BinaryExp(+,AccessExp(Na
                Some(NameDefinition(l11))
                  NameDefinition(l11)
                List()
                AssignAction(Vector(),AccessExp(NameExp(NameUser(FI)),NameUser(Counter)),:=,BinaryExp(+,AccessExp(NameExp(NameUser(FI)),NameUser(Counter)),LiteralExp(
                  List()
                  AccessExp(NameExp(NameUser(FI)),NameUser(Counter))
                    NameExp(NameUser(FI))
                      NameUser(FI)
                    NameUser(Counter)
                  BinaryExp(+,AccessExp(NameExp(NameUser(FI)),NameUser(Counter)),LiteralExp(INTEGER,1,1ii))
                    AccessExp(NameExp(NameUser(FI)),NameUser(Counter))
                      NameExp(NameUser(FI))
                        NameUser(FI)
                      NameUser(Counter)
                    LiteralExp(INTEGER,1,1ii)
              EmptyLocation(Some(NameDefinition(l6)),Vector())
                Some(NameDefinition(l6))
                  NameDefinition(l6)
                List()
              JumpLocation(Some(NameDefinition(l12)),Vector(),GotoJump(Vector(),NameUser(l3)))
                Some(NameDefinition(l12))
                  NameDefinition(l12)
                List()
                GotoJump(Vector(),NameUser(l3))
                  List()
                  NameUser(l3)
              EmptyLocation(Some(NameDefinition(l4)),Vector())
                Some(NameDefinition(l4))
                  NameDefinition(l4)
                List()
              JumpLocation(Some(NameDefinition(l15)),Vector(),IfJump(Vector(),Vector(IfThenJump(UnaryExp(!,BinaryExp(==,AccessExp(NameExp(NameUser(FI)),NameUser(Cou
                Some(NameDefinition(l15))
                  NameDefinition(l15)
                List()
                IfJump(Vector(),Vector(IfThenJump(UnaryExp(!,BinaryExp(==,AccessExp(NameExp(NameUser(FI)),NameUser(Counter)),LiteralExp(INTEGER,0,0ii))),Vector(),Name
                  List()
                  Vector(IfThenJump(UnaryExp(!,BinaryExp(==,AccessExp(NameExp(NameUser(FI)),NameUser(Counter)),LiteralExp(INTEGER,0,0ii))),Vector(),NameUser(l14)))
                    IfThenJump(UnaryExp(!,BinaryExp(==,AccessExp(NameExp(NameUser(FI)),NameUser(Counter)),LiteralExp(INTEGER,0,0ii))),Vector(),NameUser(l14))
                      UnaryExp(!,BinaryExp(==,AccessExp(NameExp(NameUser(FI)),NameUser(Counter)),LiteralExp(INTEGER,0,0ii)))
                        BinaryExp(==,AccessExp(NameExp(NameUser(FI)),NameUser(Counter)),LiteralExp(INTEGER,0,0ii))
                          AccessExp(NameExp(NameUser(FI)),NameUser(Counter))
                            NameExp(NameUser(FI))
                              NameUser(FI)
                            NameUser(Counter)
                          LiteralExp(INTEGER,0,0ii)
                      List()
                      NameUser(l14)
                  None
              ActionLocation(Some(NameDefinition(l16)),Vector(),AssignAction(Vector(),AccessExp(NameExp(NameUser(FI)),NameUser(Tripped)),:=,LiteralExp(BOOLEAN,false
                Some(NameDefinition(l16))
                  NameDefinition(l16)
                List()
                AssignAction(Vector(),AccessExp(NameExp(NameUser(FI)),NameUser(Tripped)),:=,LiteralExp(BOOLEAN,false,false))
                  List()
                  AccessExp(NameExp(NameUser(FI)),NameUser(Tripped))
                    NameExp(NameUser(FI))
                      NameUser(FI)
                    NameUser(Tripped)
                  LiteralExp(BOOLEAN,false,false)
              JumpLocation(Some(NameDefinition(l17)),Vector(),GotoJump(Vector(),NameUser(l13)))
                Some(NameDefinition(l17))
                  NameDefinition(l17)
                List()
                GotoJump(Vector(),NameUser(l13))
                  List()
                  NameUser(l13)
              EmptyLocation(Some(NameDefinition(l14)),Vector())
                Some(NameDefinition(l14))
                  NameDefinition(l14)
                List()
              ActionLocation(Some(NameDefinition(l18)),Vector(),AssignAction(Vector(),AccessExp(NameExp(NameUser(FI)),NameUser(Counter)),:=,BinaryExp(-,AccessExp(Na
                Some(NameDefinition(l18))
                  NameDefinition(l18)
                List()
                AssignAction(Vector(),AccessExp(NameExp(NameUser(FI)),NameUser(Counter)),:=,BinaryExp(-,AccessExp(NameExp(NameUser(FI)),NameUser(Counter)),LiteralExp(
                  List()
                  AccessExp(NameExp(NameUser(FI)),NameUser(Counter))
                    NameExp(NameUser(FI))
                      NameUser(FI)
                    NameUser(Counter)
                  BinaryExp(-,AccessExp(NameExp(NameUser(FI)),NameUser(Counter)),LiteralExp(INTEGER,1,1ii))
                    AccessExp(NameExp(NameUser(FI)),NameUser(Counter))
                      NameExp(NameUser(FI))
                        NameUser(FI)
                      NameUser(Counter)
                    LiteralExp(INTEGER,1,1ii)
              EmptyLocation(Some(NameDefinition(l13)),Vector())
                Some(NameDefinition(l13))
                  NameDefinition(l13)
                List()
              EmptyLocation(Some(NameDefinition(l3)),Vector())
                Some(NameDefinition(l3))
                  NameDefinition(l3)
                List()
              ActionLocation(Some(NameDefinition(l19)),Vector(),AssignAction(Vector(),NameExp(NameUser(IntegratedEvent)),:=,AccessExp(NameExp(NameUser(FI)),NameUser
                Some(NameDefinition(l19))
                  NameDefinition(l19)
                List()
                AssignAction(Vector(),NameExp(NameUser(IntegratedEvent)),:=,AccessExp(NameExp(NameUser(FI)),NameUser(Tripped)))
                  List()
                  NameExp(NameUser(IntegratedEvent))
                    NameUser(IntegratedEvent)
                  AccessExp(NameExp(NameUser(FI)),NameUser(Tripped))
                    NameExp(NameUser(FI))
                      NameUser(FI)
                    NameUser(Tripped)
              JumpLocation(Some(NameDefinition(l20)),Vector(),ReturnJump(Vector(),None))
                Some(NameDefinition(l20))
                  NameDefinition(l20)
                List()
                ReturnJump(Vector(),None)
                  List()
                  None
            List()
Model(Some(faultintegrator.ads),Vector(),Vector(PackageDecl(Some(NameDefinition(FaultIntegrator)),Vector(),List(RecordDecl(NameDefinition(T),Vector(),
  Some(faultintegrator.ads)
  List()
  Vector(PackageDecl(Some(NameDefinition(FaultIntegrator)),Vector(),List(RecordDecl(NameDefinition(T),Vector(),List(),List(),Vector(AttributeDecl(NameDe
    PackageDecl(Some(NameDefinition(FaultIntegrator)),Vector(),List(RecordDecl(NameDefinition(T),Vector(),List(),List(),Vector(AttributeDecl(NameDefinitio
      Some(NameDefinition(FaultIntegrator))
        NameDefinition(FaultIntegrator)
      List()
      List(RecordDecl(NameDefinition(T),Vector(),List(),List(),Vector(AttributeDecl(NameDefinition(Limit),Vector(),Some(NamedTypeSpec(NameUser(Positive),Lis
        RecordDecl(NameDefinition(T),Vector(),List(),List(),Vector(AttributeDecl(NameDefinition(Limit),Vector(),Some(NamedTypeSpec(NameUser(Positive),List()))
          NameDefinition(T)
          List()
          List()
          List()
          Vector(AttributeDecl(NameDefinition(Limit),Vector(),Some(NamedTypeSpec(NameUser(Positive),List())),None), AttributeDecl(NameDefinition(Counter),Vector
            AttributeDecl(NameDefinition(Limit),Vector(),Some(NamedTypeSpec(NameUser(Positive),List())),None)
              NameDefinition(Limit)
              List()
              Some(NamedTypeSpec(NameUser(Positive),List()))
                NamedTypeSpec(NameUser(Positive),List())
                  NameUser(Positive)
                  List()
              None
            AttributeDecl(NameDefinition(Counter),Vector(),Some(NamedTypeSpec(NameUser(Natural),List())),None)
              NameDefinition(Counter)
              List()
              Some(NamedTypeSpec(NameUser(Natural),List()))
                NamedTypeSpec(NameUser(Natural),List())
                  NameUser(Natural)
                  List()
              None
            AttributeDecl(NameDefinition(Tripped),Vector(),Some(NamedTypeSpec(NameUser(Boolean),List())),None)
              NameDefinition(Tripped)
              List()
              Some(NamedTypeSpec(NameUser(Boolean),List()))
                NamedTypeSpec(NameUser(Boolean),List())
                  NameUser(Boolean)
                  List()
              None
        TypeAliasDecl(NameDefinition(T),Vector(),NamedTypeSpec(NameUser(_PRIVATE_TYPE_DECLARATION_),List()))
          NameDefinition(T)
          List()
          NamedTypeSpec(NameUser(_PRIVATE_TYPE_DECLARATION_),List())
            NameUser(_PRIVATE_TYPE_DECLARATION_)
            List()
        ProcedureDecl(NameDefinition(Init),Vector(),Vector(),List(ParamDecl(Some(NamedTypeSpec(NameUser(T),Vector())),NameDefinition(FI),Vector()), ParamDecl(
          NameDefinition(Init)
          List()
          List()
          List(ParamDecl(Some(NamedTypeSpec(NameUser(T),Vector())),NameDefinition(FI),Vector()), ParamDecl(Some(NamedTypeSpec(NameUser(Positive),Vector())),Name
            ParamDecl(Some(NamedTypeSpec(NameUser(T),Vector())),NameDefinition(FI),Vector())
              Some(NamedTypeSpec(NameUser(T),Vector()))
                NamedTypeSpec(NameUser(T),Vector())
                  NameUser(T)
                  List()
              NameDefinition(FI)
              List()
            ParamDecl(Some(NamedTypeSpec(NameUser(Positive),Vector())),NameDefinition(Threshold),Vector())
              Some(NamedTypeSpec(NameUser(Positive),Vector()))
                NamedTypeSpec(NameUser(Positive),Vector())
                  NameUser(Positive)
                  List()
              NameDefinition(Threshold)
              List()
          None
          EmptyBody()
        ProcedureDecl(NameDefinition(Test),Vector(),Vector(),List(ParamDecl(Some(NamedTypeSpec(NameUser(T),Vector())),NameDefinition(FI),Vector()), ParamDecl(
          NameDefinition(Test)
          List()
          List()
          List(ParamDecl(Some(NamedTypeSpec(NameUser(T),Vector())),NameDefinition(FI),Vector()), ParamDecl(Some(NamedTypeSpec(NameUser(Boolean),Vector())),NameD
            ParamDecl(Some(NamedTypeSpec(NameUser(T),Vector())),NameDefinition(FI),Vector())
              Some(NamedTypeSpec(NameUser(T),Vector()))
                NamedTypeSpec(NameUser(T),Vector())
                  NameUser(T)
                  List()
              NameDefinition(FI)
              List()
            ParamDecl(Some(NamedTypeSpec(NameUser(Boolean),Vector())),NameDefinition(CurrentEvent),Vector())
              Some(NamedTypeSpec(NameUser(Boolean),Vector()))
                NamedTypeSpec(NameUser(Boolean),Vector())
                  NameUser(Boolean)
                  List()
              NameDefinition(CurrentEvent)
              List()
            ParamDecl(Some(NamedTypeSpec(NameUser(Boolean),Vector())),NameDefinition(IntegratedEvent),Vector())
              Some(NamedTypeSpec(NameUser(Boolean),Vector()))
                NamedTypeSpec(NameUser(Boolean),Vector())
                  NameUser(Boolean)
                  List()
              NameDefinition(IntegratedEvent)
              List()
          None
          EmptyBody()
Model(Some(fake/standard.ads),Vector(),Vector(PackageDecl(Some(NameDefinition(Standard)),Vector(),List(TypeAliasDecl(NameDefinition(Boolean),Vector(),
  Some(fake/standard.ads)
  List()
  Vector(PackageDecl(Some(NameDefinition(Standard)),Vector(),List(TypeAliasDecl(NameDefinition(Boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List())
    PackageDecl(Some(NameDefinition(Standard)),Vector(),List(TypeAliasDecl(NameDefinition(Boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List())), Type
      Some(NameDefinition(Standard))
        NameDefinition(Standard)
      List()
      List(TypeAliasDecl(NameDefinition(Boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List())), TypeAliasDecl(NameDefinition(Integer),Vector(),NamedType
        TypeAliasDecl(NameDefinition(Boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List()))
          NameDefinition(Boolean)
          List()
          NamedTypeSpec(NameUser(Boolean),List())
            NameUser(Boolean)
            List()
        TypeAliasDecl(NameDefinition(Integer),Vector(),NamedTypeSpec(NameUser(Integer),List()))
          NameDefinition(Integer)
          List()
          NamedTypeSpec(NameUser(Integer),List())
            NameUser(Integer)
            List()
        TypeAliasDecl(NameDefinition(Natural),Vector(),NamedTypeSpec(NameUser(Integer),List()))
          NameDefinition(Natural)
          List()
          NamedTypeSpec(NameUser(Integer),List())
            NameUser(Integer)
            List()
        TypeAliasDecl(NameDefinition(Positive),Vector(),NamedTypeSpec(NameUser(Integer),List()))
          NameDefinition(Positive)
          List()
          NamedTypeSpec(NameUser(Integer),List())
            NameUser(Integer)
            List()
        TypeAliasDecl(NameDefinition(Float),Vector(),NamedTypeSpec(NameUser(Float),List()))
          NameDefinition(Float)
          List()
          NamedTypeSpec(NameUser(Float),List())
            NameUser(Float)
            List()
        TypeAliasDecl(NameDefinition(universal_integer),Vector(),NamedTypeSpec(NameUser(universal_integer),List()))
          NameDefinition(universal_integer)
          List()
          NamedTypeSpec(NameUser(universal_integer),List())
            NameUser(universal_integer)
            List()
        TypeAliasDecl(NameDefinition(universal_real),Vector(),NamedTypeSpec(NameUser(universal_real),List()))
          NameDefinition(universal_real)
          List()
          NamedTypeSpec(NameUser(universal_real),List())
            NameUser(universal_real)
            List()
        TypeAliasDecl(NameDefinition(Character),Vector(),NamedTypeSpec(NameUser(Character),List()))
          NameDefinition(Character)
          List()
          NamedTypeSpec(NameUser(Character),List())
            NameUser(Character)
            List()
        TypeAliasDecl(NameDefinition(String),Vector(),NamedTypeSpec(NameUser(String),List()))
          NameDefinition(String)
          List()
          NamedTypeSpec(NameUser(String),List())
            NameUser(String)
            List()

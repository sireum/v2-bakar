Model(Some(example.adb),Vector(),Vector(PackageDecl(Some(NameDefinition(example)),Vector(),List(TypeAliasDecl(NameDefinition(standard::boolean),Vector
  Some(example.adb)
  List()
  Vector(PackageDecl(Some(NameDefinition(example)),Vector(),List(TypeAliasDecl(NameDefinition(standard::boolean),Vector(),NamedTypeSpec(NameUser(Boolean
    PackageDecl(Some(NameDefinition(example)),Vector(),List(TypeAliasDecl(NameDefinition(standard::boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List(
      Some(NameDefinition(example))
        NameDefinition(example)
      List()
      List(TypeAliasDecl(NameDefinition(standard::boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List())), TypeAliasDecl(NameDefinition(standard::integer
        TypeAliasDecl(NameDefinition(standard::boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List()))
          NameDefinition(standard::boolean)
          List()
          NamedTypeSpec(NameUser(Boolean),List())
            NameUser(Boolean)
            List()
        TypeAliasDecl(NameDefinition(standard::integer),Vector(),NamedTypeSpec(NameUser(Integer),List()))
          NameDefinition(standard::integer)
          List()
          NamedTypeSpec(NameUser(Integer),List())
            NameUser(Integer)
            List()
        TypeAliasDecl(NameDefinition(standard::natural),Vector(),NamedTypeSpec(NameUser(Integer),List()))
          NameDefinition(standard::natural)
          List()
          NamedTypeSpec(NameUser(Integer),List())
            NameUser(Integer)
            List()
        TypeAliasDecl(NameDefinition(standard::positive),Vector(),NamedTypeSpec(NameUser(Integer),List()))
          NameDefinition(standard::positive)
          List()
          NamedTypeSpec(NameUser(Integer),List())
            NameUser(Integer)
            List()
        TypeAliasDecl(NameDefinition(standard::float),Vector(),NamedTypeSpec(NameUser(Float),List()))
          NameDefinition(standard::float)
          List()
          NamedTypeSpec(NameUser(Float),List())
            NameUser(Float)
            List()
        TypeAliasDecl(NameDefinition(universal_integer),Vector(),NamedTypeSpec(NameUser(Integer),List()))
          NameDefinition(universal_integer)
          List()
          NamedTypeSpec(NameUser(Integer),List())
            NameUser(Integer)
            List()
        ProcedureDecl(NameDefinition(add),Vector(),Vector(),List(ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(x),Vector()), ParamD
          NameDefinition(add)
          List()
          List()
          List(ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(x),Vector()), ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),
            ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(x),Vector())
              Some(NamedTypeSpec(NameUser(Integer),Vector()))
                NamedTypeSpec(NameUser(Integer),Vector())
                  NameUser(Integer)
                  List()
              NameDefinition(x)
              List()
            ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(y),Vector())
              Some(NamedTypeSpec(NameUser(Integer),Vector()))
                NamedTypeSpec(NameUser(Integer),Vector())
                  NameUser(Integer)
                  List()
              NameDefinition(y)
              List()
          None
          ImplementedBody(List(),List(ActionLocation(Some(NameDefinition(l1)),Vector(),AssignAction(Vector(),NameExp(NameUser(x)),:=,BinaryExp(+,NameExp(NameUse
            List()
            List(ActionLocation(Some(NameDefinition(l1)),Vector(),AssignAction(Vector(),NameExp(NameUser(x)),:=,BinaryExp(+,NameExp(NameUser(x)),NameExp(NameUser(
              ActionLocation(Some(NameDefinition(l1)),Vector(),AssignAction(Vector(),NameExp(NameUser(x)),:=,BinaryExp(+,NameExp(NameUser(x)),NameExp(NameUser(y))))
                Some(NameDefinition(l1))
                  NameDefinition(l1)
                List()
                AssignAction(Vector(),NameExp(NameUser(x)),:=,BinaryExp(+,NameExp(NameUser(x)),NameExp(NameUser(y))))
                  List()
                  NameExp(NameUser(x))
                    NameUser(x)
                  BinaryExp(+,NameExp(NameUser(x)),NameExp(NameUser(y)))
                    NameExp(NameUser(x))
                      NameUser(x)
                    NameExp(NameUser(y))
                      NameUser(y)
              JumpLocation(Some(NameDefinition(l2)),Vector(),ReturnJump(Vector(),None))
                Some(NameDefinition(l2))
                  NameDefinition(l2)
                List()
                ReturnJump(Vector(),None)
                  List()
                  None
            List()
        ProcedureDecl(NameDefinition(foo),Vector(),Vector(),List(ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(A),Vector()), ParamD
          NameDefinition(foo)
          List()
          List()
          List(ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(A),Vector()), ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),
            ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(A),Vector())
              Some(NamedTypeSpec(NameUser(Integer),Vector()))
                NamedTypeSpec(NameUser(Integer),Vector())
                  NameUser(Integer)
                  List()
              NameDefinition(A)
              List()
            ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(B),Vector())
              Some(NamedTypeSpec(NameUser(Integer),Vector()))
                NamedTypeSpec(NameUser(Integer),Vector())
                  NameUser(Integer)
                  List()
              NameDefinition(B)
              List()
          None
          ImplementedBody(List(LocalVarDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(M),Vector())),List(ActionLocation(Some(NameDefinition
            List(LocalVarDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(M),Vector()))
              LocalVarDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(M),Vector())
                Some(NamedTypeSpec(NameUser(Integer),Vector()))
                  NamedTypeSpec(NameUser(Integer),Vector())
                    NameUser(Integer)
                    List()
                NameDefinition(M)
                List()
            List(ActionLocation(Some(NameDefinition(l3)),Vector(),AssignAction(Vector(),NameExp(NameUser(M)),:=,LiteralExp(INTEGER,3,3ii))), ActionLocation(Some(N
              ActionLocation(Some(NameDefinition(l3)),Vector(),AssignAction(Vector(),NameExp(NameUser(M)),:=,LiteralExp(INTEGER,3,3ii)))
                Some(NameDefinition(l3))
                  NameDefinition(l3)
                List()
                AssignAction(Vector(),NameExp(NameUser(M)),:=,LiteralExp(INTEGER,3,3ii))
                  List()
                  NameExp(NameUser(M))
                    NameUser(M)
                  LiteralExp(INTEGER,3,3ii)
              ActionLocation(Some(NameDefinition(l4)),Vector(),AssignAction(Vector(),NameExp(NameUser(A)),:=,BinaryExp(+,NameExp(NameUser(A)),LiteralExp(INTEGER,2,2
                Some(NameDefinition(l4))
                  NameDefinition(l4)
                List()
                AssignAction(Vector(),NameExp(NameUser(A)),:=,BinaryExp(+,NameExp(NameUser(A)),LiteralExp(INTEGER,2,2ii)))
                  List()
                  NameExp(NameUser(A))
                    NameUser(A)
                  BinaryExp(+,NameExp(NameUser(A)),LiteralExp(INTEGER,2,2ii))
                    NameExp(NameUser(A))
                      NameUser(A)
                    LiteralExp(INTEGER,2,2ii)
              ActionLocation(Some(NameDefinition(l5)),Vector(),AssignAction(Vector(),NameExp(NameUser(B)),:=,BinaryExp(+,NameExp(NameUser(B)),LiteralExp(INTEGER,5,5
                Some(NameDefinition(l5))
                  NameDefinition(l5)
                List()
                AssignAction(Vector(),NameExp(NameUser(B)),:=,BinaryExp(+,NameExp(NameUser(B)),LiteralExp(INTEGER,5,5ii)))
                  List()
                  NameExp(NameUser(B))
                    NameUser(B)
                  BinaryExp(+,NameExp(NameUser(B)),LiteralExp(INTEGER,5,5ii))
                    NameExp(NameUser(B))
                      NameUser(B)
                    LiteralExp(INTEGER,5,5ii)
              JumpLocation(Some(NameDefinition(l6)),Vector(),ReturnJump(Vector(),None))
                Some(NameDefinition(l6))
                  NameDefinition(l6)
                List()
                ReturnJump(Vector(),None)
                  List()
                  None
            List()

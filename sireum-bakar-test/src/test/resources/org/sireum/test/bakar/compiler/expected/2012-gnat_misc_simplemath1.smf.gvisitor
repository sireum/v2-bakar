Model(Some(simplemath1.adb),Vector(),Vector(PackageDecl(Some(NameDefinition(SimpleMath1)),Vector(),List(TypeAliasDecl(NameDefinition(standard::integer
  Some(simplemath1.adb)
  List()
  Vector(PackageDecl(Some(NameDefinition(SimpleMath1)),Vector(),List(TypeAliasDecl(NameDefinition(standard::integer),Vector(),NamedTypeSpec(NameUser(),L
    PackageDecl(Some(NameDefinition(SimpleMath1)),Vector(),List(TypeAliasDecl(NameDefinition(standard::integer),Vector(),NamedTypeSpec(NameUser(),List()))
      Some(NameDefinition(SimpleMath1))
        NameDefinition(SimpleMath1)
      List()
      List(TypeAliasDecl(NameDefinition(standard::integer),Vector(),NamedTypeSpec(NameUser(),List())), ProcedureDecl(NameDefinition(Inc),Vector(),Vector(),L
        TypeAliasDecl(NameDefinition(standard::integer),Vector(),NamedTypeSpec(NameUser(),List()))
          NameDefinition(standard::integer)
          List()
          NamedTypeSpec(NameUser(),List())
            NameUser()
            List()
        ProcedureDecl(NameDefinition(Inc),Vector(),Vector(),List(ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(I),Vector())),None,f
          NameDefinition(Inc)
          List()
          List()
          List(ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(I),Vector()))
            ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(I),Vector())
              Some(NamedTypeSpec(NameUser(Integer),Vector()))
                NamedTypeSpec(NameUser(Integer),Vector())
                  NameUser(Integer)
                  List()
              NameDefinition(I)
              List()
          None
          ImplementedBody(List(),List(ActionLocation(Some(NameDefinition(l1)),Vector(),AssignAction(Vector(),NameExp(NameUser(I)),:=,BinaryExp(+,NameExp(NameUse
            List()
            List(ActionLocation(Some(NameDefinition(l1)),Vector(),AssignAction(Vector(),NameExp(NameUser(I)),:=,BinaryExp(+,NameExp(NameUser(I)),NameExp(NameUser(
              ActionLocation(Some(NameDefinition(l1)),Vector(),AssignAction(Vector(),NameExp(NameUser(I)),:=,BinaryExp(+,NameExp(NameUser(I)),NameExp(NameUser(B))))
                Some(NameDefinition(l1))
                  NameDefinition(l1)
                List()
                AssignAction(Vector(),NameExp(NameUser(I)),:=,BinaryExp(+,NameExp(NameUser(I)),NameExp(NameUser(B))))
                  List()
                  NameExp(NameUser(I))
                    NameUser(I)
                  BinaryExp(+,NameExp(NameUser(I)),NameExp(NameUser(B)))
                    NameExp(NameUser(I))
                      NameUser(I)
                    NameExp(NameUser(B))
                      NameUser(B)
              JumpLocation(Some(NameDefinition(l2)),Vector(),ReturnJump(Vector(),None))
                Some(NameDefinition(l2))
                  NameDefinition(l2)
                List()
                ReturnJump(Vector(),None)
                  List()
                  None
            List()

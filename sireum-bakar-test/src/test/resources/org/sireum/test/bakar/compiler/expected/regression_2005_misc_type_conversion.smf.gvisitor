Model(Some(type_conversion.adb),Vector(),Vector(PackageDecl(Some(NameDefinition(Type_Conversion)),Vector(),List(ProcedureDecl(NameDefinition(Convert),
  Some(type_conversion.adb)
  List()
  Vector(PackageDecl(Some(NameDefinition(Type_Conversion)),Vector(),List(ProcedureDecl(NameDefinition(Convert),Vector(),Vector(),List(ParamDecl(Some(Nam
    PackageDecl(Some(NameDefinition(Type_Conversion)),Vector(),List(ProcedureDecl(NameDefinition(Convert),Vector(),Vector(),List(ParamDecl(Some(NamedTypeS
      Some(NameDefinition(Type_Conversion))
        NameDefinition(Type_Conversion)
      List()
      List(ProcedureDecl(NameDefinition(Convert),Vector(),Vector(),List(ParamDecl(Some(NamedTypeSpec(NameUser(Float),Vector())),NameDefinition(Y),Vector()),
        ProcedureDecl(NameDefinition(Convert),Vector(),Vector(),List(ParamDecl(Some(NamedTypeSpec(NameUser(Float),Vector())),NameDefinition(Y),Vector()), Para
          NameDefinition(Convert)
          List()
          List()
          List(ParamDecl(Some(NamedTypeSpec(NameUser(Float),Vector())),NameDefinition(Y),Vector()), ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),Na
            ParamDecl(Some(NamedTypeSpec(NameUser(Float),Vector())),NameDefinition(Y),Vector())
              Some(NamedTypeSpec(NameUser(Float),Vector()))
                NamedTypeSpec(NameUser(Float),Vector())
                  NameUser(Float)
                  List()
              NameDefinition(Y)
              List()
            ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(X),Vector())
              Some(NamedTypeSpec(NameUser(Integer),Vector()))
                NamedTypeSpec(NameUser(Integer),Vector())
                  NameUser(Integer)
                  List()
              NameDefinition(X)
              List()
          None
          ImplementedBody(List(),List(ActionLocation(Some(NameDefinition(l1)),Vector(),AssignAction(Vector(),NameExp(NameUser(X)),:=,CastExp(NamedTypeSpec(NameU
            List()
            List(ActionLocation(Some(NameDefinition(l1)),Vector(),AssignAction(Vector(),NameExp(NameUser(X)),:=,CastExp(NamedTypeSpec(NameUser(Integer),Vector()),
              ActionLocation(Some(NameDefinition(l1)),Vector(),AssignAction(Vector(),NameExp(NameUser(X)),:=,CastExp(NamedTypeSpec(NameUser(Integer),Vector()),NameE
                Some(NameDefinition(l1))
                  NameDefinition(l1)
                List()
                AssignAction(Vector(),NameExp(NameUser(X)),:=,CastExp(NamedTypeSpec(NameUser(Integer),Vector()),NameExp(NameUser(Y))))
                  List()
                  NameExp(NameUser(X))
                    NameUser(X)
                  CastExp(NamedTypeSpec(NameUser(Integer),Vector()),NameExp(NameUser(Y)))
                    NamedTypeSpec(NameUser(Integer),Vector())
                      NameUser(Integer)
                      List()
                    NameExp(NameUser(Y))
                      NameUser(Y)
              JumpLocation(Some(NameDefinition(l2)),Vector(),ReturnJump(Vector(),None))
                Some(NameDefinition(l2))
                  NameDefinition(l2)
                List()
                ReturnJump(Vector(),None)
                  List()
                  None
            List()
Model(Some(type_conversion.ads),Vector(),Vector(PackageDecl(Some(NameDefinition(Type_Conversion)),Vector(),List(ProcedureDecl(NameDefinition(Convert),
  Some(type_conversion.ads)
  List()
  Vector(PackageDecl(Some(NameDefinition(Type_Conversion)),Vector(),List(ProcedureDecl(NameDefinition(Convert),Vector(),Vector(),List(ParamDecl(Some(Nam
    PackageDecl(Some(NameDefinition(Type_Conversion)),Vector(),List(ProcedureDecl(NameDefinition(Convert),Vector(),Vector(),List(ParamDecl(Some(NamedTypeS
      Some(NameDefinition(Type_Conversion))
        NameDefinition(Type_Conversion)
      List()
      List(ProcedureDecl(NameDefinition(Convert),Vector(),Vector(),List(ParamDecl(Some(NamedTypeSpec(NameUser(Float),Vector())),NameDefinition(Y),Vector()),
        ProcedureDecl(NameDefinition(Convert),Vector(),Vector(),List(ParamDecl(Some(NamedTypeSpec(NameUser(Float),Vector())),NameDefinition(Y),Vector()), Para
          NameDefinition(Convert)
          List()
          List()
          List(ParamDecl(Some(NamedTypeSpec(NameUser(Float),Vector())),NameDefinition(Y),Vector()), ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),Na
            ParamDecl(Some(NamedTypeSpec(NameUser(Float),Vector())),NameDefinition(Y),Vector())
              Some(NamedTypeSpec(NameUser(Float),Vector()))
                NamedTypeSpec(NameUser(Float),Vector())
                  NameUser(Float)
                  List()
              NameDefinition(Y)
              List()
            ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(X),Vector())
              Some(NamedTypeSpec(NameUser(Integer),Vector()))
                NamedTypeSpec(NameUser(Integer),Vector())
                  NameUser(Integer)
                  List()
              NameDefinition(X)
              List()
          None
          EmptyBody()
Model(Some(fake/standard.ads),Vector(),Vector(PackageDecl(Some(NameDefinition(Standard)),Vector(),List(TypeAliasDecl(NameDefinition(Boolean),Vector(),
  Some(fake/standard.ads)
  List()
  Vector(PackageDecl(Some(NameDefinition(Standard)),Vector(),List(TypeAliasDecl(NameDefinition(Boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List())
    PackageDecl(Some(NameDefinition(Standard)),Vector(),List(TypeAliasDecl(NameDefinition(Boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List())), Type
      Some(NameDefinition(Standard))
        NameDefinition(Standard)
      List()
      List(TypeAliasDecl(NameDefinition(Boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List())), TypeAliasDecl(NameDefinition(Integer),Vector(),NamedType
        TypeAliasDecl(NameDefinition(Boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List()))
          NameDefinition(Boolean)
          List()
          NamedTypeSpec(NameUser(Boolean),List())
            NameUser(Boolean)
            List()
        TypeAliasDecl(NameDefinition(Integer),Vector(),NamedTypeSpec(NameUser(Integer),List()))
          NameDefinition(Integer)
          List()
          NamedTypeSpec(NameUser(Integer),List())
            NameUser(Integer)
            List()
        TypeAliasDecl(NameDefinition(Natural),Vector(),NamedTypeSpec(NameUser(Integer),List()))
          NameDefinition(Natural)
          List()
          NamedTypeSpec(NameUser(Integer),List())
            NameUser(Integer)
            List()
        TypeAliasDecl(NameDefinition(Positive),Vector(),NamedTypeSpec(NameUser(Integer),List()))
          NameDefinition(Positive)
          List()
          NamedTypeSpec(NameUser(Integer),List())
            NameUser(Integer)
            List()
        TypeAliasDecl(NameDefinition(Float),Vector(),NamedTypeSpec(NameUser(Float),List()))
          NameDefinition(Float)
          List()
          NamedTypeSpec(NameUser(Float),List())
            NameUser(Float)
            List()
        TypeAliasDecl(NameDefinition(universal_integer),Vector(),NamedTypeSpec(NameUser(universal_integer),List()))
          NameDefinition(universal_integer)
          List()
          NamedTypeSpec(NameUser(universal_integer),List())
            NameUser(universal_integer)
            List()
        TypeAliasDecl(NameDefinition(universal_real),Vector(),NamedTypeSpec(NameUser(universal_real),List()))
          NameDefinition(universal_real)
          List()
          NamedTypeSpec(NameUser(universal_real),List())
            NameUser(universal_real)
            List()

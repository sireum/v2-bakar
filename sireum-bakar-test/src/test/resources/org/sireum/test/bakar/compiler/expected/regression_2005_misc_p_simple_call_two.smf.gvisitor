Model(Some(p_simple_call_two.adb),Vector(),Vector(PackageDecl(Some(NameDefinition(P_Simple_Call_Two)),Vector(),List(GlobalVarDecl(NameDefinition(@@B),
  Some(p_simple_call_two.adb)
  List()
  Vector(PackageDecl(Some(NameDefinition(P_Simple_Call_Two)),Vector(),List(GlobalVarDecl(NameDefinition(@@B),Vector(),Some(NamedTypeSpec(NameUser(Intege
    PackageDecl(Some(NameDefinition(P_Simple_Call_Two)),Vector(),List(GlobalVarDecl(NameDefinition(@@B),Vector(),Some(NamedTypeSpec(NameUser(Integer),Vect
      Some(NameDefinition(P_Simple_Call_Two))
        NameDefinition(P_Simple_Call_Two)
      List()
      List(GlobalVarDecl(NameDefinition(@@B),Vector(),Some(NamedTypeSpec(NameUser(Integer),Vector()))), ProcedureDecl(NameDefinition($$binit),Vector(),Vecto
        GlobalVarDecl(NameDefinition(@@B),Vector(),Some(NamedTypeSpec(NameUser(Integer),Vector())))
          NameDefinition(@@B)
          List()
          Some(NamedTypeSpec(NameUser(Integer),Vector()))
            NamedTypeSpec(NameUser(Integer),Vector())
              NameUser(Integer)
              List()
        ProcedureDecl(NameDefinition($$binit),Vector(),Vector(),Vector(),None,false,ImplementedBody(Vector(),List(ActionLocation(Some(NameDefinition(l1)),Vect
          NameDefinition($$binit)
          List()
          List()
          List()
          None
          ImplementedBody(Vector(),List(ActionLocation(Some(NameDefinition(l1)),Vector(),AssignAction(Vector(),NameExp(NameUser(Y)),:=,LiteralExp(INTEGER,1,1ii)
            List()
            List(ActionLocation(Some(NameDefinition(l1)),Vector(),AssignAction(Vector(),NameExp(NameUser(Y)),:=,LiteralExp(INTEGER,1,1ii))), ActionLocation(Some(N
              ActionLocation(Some(NameDefinition(l1)),Vector(),AssignAction(Vector(),NameExp(NameUser(Y)),:=,LiteralExp(INTEGER,1,1ii)))
                Some(NameDefinition(l1))
                  NameDefinition(l1)
                List()
                AssignAction(Vector(),NameExp(NameUser(Y)),:=,LiteralExp(INTEGER,1,1ii))
                  List()
                  NameExp(NameUser(Y))
                    NameUser(Y)
                  LiteralExp(INTEGER,1,1ii)
              ActionLocation(Some(NameDefinition(l2)),Vector(),AssignAction(Vector(),NameExp(NameUser(@@B)),:=,LiteralExp(INTEGER,0,0ii)))
                Some(NameDefinition(l2))
                  NameDefinition(l2)
                List()
                AssignAction(Vector(),NameExp(NameUser(@@B)),:=,LiteralExp(INTEGER,0,0ii))
                  List()
                  NameExp(NameUser(@@B))
                    NameUser(@@B)
                  LiteralExp(INTEGER,0,0ii)
              ActionLocation(Some(NameDefinition(l3)),Vector(),AssignAction(Vector(),NameExp(NameUser(C)),:=,LiteralExp(INTEGER,0,0ii)))
                Some(NameDefinition(l3))
                  NameDefinition(l3)
                List()
                AssignAction(Vector(),NameExp(NameUser(C)),:=,LiteralExp(INTEGER,0,0ii))
                  List()
                  NameExp(NameUser(C))
                    NameUser(C)
                  LiteralExp(INTEGER,0,0ii)
              JumpLocation(Some(NameDefinition(l4)),Vector(),ReturnJump(Vector(),None))
                Some(NameDefinition(l4))
                  NameDefinition(l4)
                List()
                ReturnJump(Vector(),None)
                  List()
                  None
            List()
        ProcedureDecl(NameDefinition(R),Vector(),Vector(),List(ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(X),Vector()), ParamDec
          NameDefinition(R)
          List()
          List()
          List(ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(X),Vector()), ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),
            ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(X),Vector())
              Some(NamedTypeSpec(NameUser(Integer),Vector()))
                NamedTypeSpec(NameUser(Integer),Vector())
                  NameUser(Integer)
                  List()
              NameDefinition(X)
              List()
            ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(Z),Vector())
              Some(NamedTypeSpec(NameUser(Integer),Vector()))
                NamedTypeSpec(NameUser(Integer),Vector())
                  NameUser(Integer)
                  List()
              NameDefinition(Z)
              List()
          None
          ImplementedBody(List(),List(ActionLocation(Some(NameDefinition(l5)),Vector(),AssignAction(Vector(),NameExp(NameUser(Z)),:=,BinaryExp(+,BinaryExp(+,Bin
            List()
            List(ActionLocation(Some(NameDefinition(l5)),Vector(),AssignAction(Vector(),NameExp(NameUser(Z)),:=,BinaryExp(+,BinaryExp(+,BinaryExp(+,NameExp(NameUs
              ActionLocation(Some(NameDefinition(l5)),Vector(),AssignAction(Vector(),NameExp(NameUser(Z)),:=,BinaryExp(+,BinaryExp(+,BinaryExp(+,NameExp(NameUser(X)
                Some(NameDefinition(l5))
                  NameDefinition(l5)
                List()
                AssignAction(Vector(),NameExp(NameUser(Z)),:=,BinaryExp(+,BinaryExp(+,BinaryExp(+,NameExp(NameUser(X)),NameExp(NameUser(Y))),NameExp(NameUser(@@B))),N
                  List()
                  NameExp(NameUser(Z))
                    NameUser(Z)
                  BinaryExp(+,BinaryExp(+,BinaryExp(+,NameExp(NameUser(X)),NameExp(NameUser(Y))),NameExp(NameUser(@@B))),NameExp(NameUser(C)))
                    BinaryExp(+,BinaryExp(+,NameExp(NameUser(X)),NameExp(NameUser(Y))),NameExp(NameUser(@@B)))
                      BinaryExp(+,NameExp(NameUser(X)),NameExp(NameUser(Y)))
                        NameExp(NameUser(X))
                          NameUser(X)
                        NameExp(NameUser(Y))
                          NameUser(Y)
                      NameExp(NameUser(@@B))
                        NameUser(@@B)
                    NameExp(NameUser(C))
                      NameUser(C)
              JumpLocation(Some(NameDefinition(l6)),Vector(),ReturnJump(Vector(),None))
                Some(NameDefinition(l6))
                  NameDefinition(l6)
                List()
                ReturnJump(Vector(),None)
                  List()
                  None
            List()
        ProcedureDecl(NameDefinition(Q),Vector(),Vector(),List(ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(X),Vector()), ParamDec
          NameDefinition(Q)
          List()
          List()
          List(ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(X),Vector()), ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),
            ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(X),Vector())
              Some(NamedTypeSpec(NameUser(Integer),Vector()))
                NamedTypeSpec(NameUser(Integer),Vector())
                  NameUser(Integer)
                  List()
              NameDefinition(X)
              List()
            ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(Z),Vector())
              Some(NamedTypeSpec(NameUser(Integer),Vector()))
                NamedTypeSpec(NameUser(Integer),Vector())
                  NameUser(Integer)
                  List()
              NameDefinition(Z)
              List()
          None
          ImplementedBody(List(),List(JumpLocation(Some(NameDefinition(l7)),Vector(),CallJump(Vector(),Vector(),CallExp(NameExp(NameUser(R)),TupleExp(List(NameE
            List()
            List(JumpLocation(Some(NameDefinition(l7)),Vector(),CallJump(Vector(),Vector(),CallExp(NameExp(NameUser(R)),TupleExp(List(NameExp(NameUser(X)), NameEx
              JumpLocation(Some(NameDefinition(l7)),Vector(),CallJump(Vector(),Vector(),CallExp(NameExp(NameUser(R)),TupleExp(List(NameExp(NameUser(X)), NameExp(Nam
                Some(NameDefinition(l7))
                  NameDefinition(l7)
                List()
                CallJump(Vector(),Vector(),CallExp(NameExp(NameUser(R)),TupleExp(List(NameExp(NameUser(X)), NameExp(NameUser(Z))))),None)
                  List()
                  List()
                  CallExp(NameExp(NameUser(R)),TupleExp(List(NameExp(NameUser(X)), NameExp(NameUser(Z)))))
                    NameExp(NameUser(R))
                      NameUser(R)
                    TupleExp(List(NameExp(NameUser(X)), NameExp(NameUser(Z))))
                      List(NameExp(NameUser(X)), NameExp(NameUser(Z)))
                        NameExp(NameUser(X))
                          NameUser(X)
                        NameExp(NameUser(Z))
                          NameUser(Z)
                  None
              ActionLocation(Some(NameDefinition(l8)),Vector(),AssignAction(Vector(),NameExp(NameUser(Z)),:=,BinaryExp(+,NameExp(NameUser(Z)),NameExp(NameUser(X))))
                Some(NameDefinition(l8))
                  NameDefinition(l8)
                List()
                AssignAction(Vector(),NameExp(NameUser(Z)),:=,BinaryExp(+,NameExp(NameUser(Z)),NameExp(NameUser(X))))
                  List()
                  NameExp(NameUser(Z))
                    NameUser(Z)
                  BinaryExp(+,NameExp(NameUser(Z)),NameExp(NameUser(X)))
                    NameExp(NameUser(Z))
                      NameUser(Z)
                    NameExp(NameUser(X))
                      NameUser(X)
              JumpLocation(Some(NameDefinition(l9)),Vector(),ReturnJump(Vector(),None))
                Some(NameDefinition(l9))
                  NameDefinition(l9)
                List()
                ReturnJump(Vector(),None)
                  List()
                  None
            List()
Model(Some(p_simple_call_two.ads),Vector(),Vector(PackageDecl(Some(NameDefinition(P_Simple_Call_Two)),Vector(),List(GlobalVarDecl(NameDefinition(@@Y),
  Some(p_simple_call_two.ads)
  List()
  Vector(PackageDecl(Some(NameDefinition(P_Simple_Call_Two)),Vector(),List(GlobalVarDecl(NameDefinition(@@Y),Vector(),Some(NamedTypeSpec(NameUser(Intege
    PackageDecl(Some(NameDefinition(P_Simple_Call_Two)),Vector(),List(GlobalVarDecl(NameDefinition(@@Y),Vector(),Some(NamedTypeSpec(NameUser(Integer),Vect
      Some(NameDefinition(P_Simple_Call_Two))
        NameDefinition(P_Simple_Call_Two)
      List()
      List(GlobalVarDecl(NameDefinition(@@Y),Vector(),Some(NamedTypeSpec(NameUser(Integer),Vector()))), GlobalVarDecl(NameDefinition(@@C),Vector(),Some(Name
        GlobalVarDecl(NameDefinition(@@Y),Vector(),Some(NamedTypeSpec(NameUser(Integer),Vector())))
          NameDefinition(@@Y)
          List()
          Some(NamedTypeSpec(NameUser(Integer),Vector()))
            NamedTypeSpec(NameUser(Integer),Vector())
              NameUser(Integer)
              List()
        GlobalVarDecl(NameDefinition(@@C),Vector(),Some(NamedTypeSpec(NameUser(Integer),Vector())))
          NameDefinition(@@C)
          List()
          Some(NamedTypeSpec(NameUser(Integer),Vector()))
            NamedTypeSpec(NameUser(Integer),Vector())
              NameUser(Integer)
              List()
        ProcedureDecl(NameDefinition(Q),Vector(),Vector(),List(ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(X),Vector()), ParamDec
          NameDefinition(Q)
          List()
          List()
          List(ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(X),Vector()), ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),
            ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(X),Vector())
              Some(NamedTypeSpec(NameUser(Integer),Vector()))
                NamedTypeSpec(NameUser(Integer),Vector())
                  NameUser(Integer)
                  List()
              NameDefinition(X)
              List()
            ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(Z),Vector())
              Some(NamedTypeSpec(NameUser(Integer),Vector()))
                NamedTypeSpec(NameUser(Integer),Vector())
                  NameUser(Integer)
                  List()
              NameDefinition(Z)
              List()
          None
          EmptyBody()
        ProcedureDecl(NameDefinition(R),Vector(),Vector(),List(ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(X),Vector()), ParamDec
          NameDefinition(R)
          List()
          List()
          List(ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(X),Vector()), ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),
            ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(X),Vector())
              Some(NamedTypeSpec(NameUser(Integer),Vector()))
                NamedTypeSpec(NameUser(Integer),Vector())
                  NameUser(Integer)
                  List()
              NameDefinition(X)
              List()
            ParamDecl(Some(NamedTypeSpec(NameUser(Integer),Vector())),NameDefinition(Z),Vector())
              Some(NamedTypeSpec(NameUser(Integer),Vector()))
                NamedTypeSpec(NameUser(Integer),Vector())
                  NameUser(Integer)
                  List()
              NameDefinition(Z)
              List()
          None
          EmptyBody()
Model(Some(fake/standard.ads),Vector(),Vector(PackageDecl(Some(NameDefinition(Standard)),Vector(),List(TypeAliasDecl(NameDefinition(Boolean),Vector(),
  Some(fake/standard.ads)
  List()
  Vector(PackageDecl(Some(NameDefinition(Standard)),Vector(),List(TypeAliasDecl(NameDefinition(Boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List())
    PackageDecl(Some(NameDefinition(Standard)),Vector(),List(TypeAliasDecl(NameDefinition(Boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List())), Type
      Some(NameDefinition(Standard))
        NameDefinition(Standard)
      List()
      List(TypeAliasDecl(NameDefinition(Boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List())), TypeAliasDecl(NameDefinition(Integer),Vector(),NamedType
        TypeAliasDecl(NameDefinition(Boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List()))
          NameDefinition(Boolean)
          List()
          NamedTypeSpec(NameUser(Boolean),List())
            NameUser(Boolean)
            List()
        TypeAliasDecl(NameDefinition(Integer),Vector(),NamedTypeSpec(NameUser(Integer),List()))
          NameDefinition(Integer)
          List()
          NamedTypeSpec(NameUser(Integer),List())
            NameUser(Integer)
            List()
        TypeAliasDecl(NameDefinition(Natural),Vector(),NamedTypeSpec(NameUser(Integer),List()))
          NameDefinition(Natural)
          List()
          NamedTypeSpec(NameUser(Integer),List())
            NameUser(Integer)
            List()
        TypeAliasDecl(NameDefinition(Positive),Vector(),NamedTypeSpec(NameUser(Integer),List()))
          NameDefinition(Positive)
          List()
          NamedTypeSpec(NameUser(Integer),List())
            NameUser(Integer)
            List()
        TypeAliasDecl(NameDefinition(Float),Vector(),NamedTypeSpec(NameUser(Float),List()))
          NameDefinition(Float)
          List()
          NamedTypeSpec(NameUser(Float),List())
            NameUser(Float)
            List()
        TypeAliasDecl(NameDefinition(universal_integer),Vector(),NamedTypeSpec(NameUser(universal_integer),List()))
          NameDefinition(universal_integer)
          List()
          NamedTypeSpec(NameUser(universal_integer),List())
            NameUser(universal_integer)
            List()
        TypeAliasDecl(NameDefinition(universal_real),Vector(),NamedTypeSpec(NameUser(universal_real),List()))
          NameDefinition(universal_real)
          List()
          NamedTypeSpec(NameUser(universal_real),List())
            NameUser(universal_real)
            List()

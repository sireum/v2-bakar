Model(Some(case_statement_enum.adb),Vector(),Vector(PackageDecl(Some(NameDefinition(Case_Statement_Enum)),Vector(),List(ProcedureDecl(NameDefinition(i
  Some(case_statement_enum.adb)
  List()
  Vector(PackageDecl(Some(NameDefinition(Case_Statement_Enum)),Vector(),List(ProcedureDecl(NameDefinition(isWorkDay),Vector(),Vector(),List(ParamDecl(So
    PackageDecl(Some(NameDefinition(Case_Statement_Enum)),Vector(),List(ProcedureDecl(NameDefinition(isWorkDay),Vector(),Vector(),List(ParamDecl(Some(Name
      Some(NameDefinition(Case_Statement_Enum))
        NameDefinition(Case_Statement_Enum)
      List()
      List(ProcedureDecl(NameDefinition(isWorkDay),Vector(),Vector(),List(ParamDecl(Some(NamedTypeSpec(NameUser(Week),Vector())),NameDefinition(d),Vector())
        ProcedureDecl(NameDefinition(isWorkDay),Vector(),Vector(),List(ParamDecl(Some(NamedTypeSpec(NameUser(Week),Vector())),NameDefinition(d),Vector())),Som
          NameDefinition(isWorkDay)
          List()
          List()
          List(ParamDecl(Some(NamedTypeSpec(NameUser(Week),Vector())),NameDefinition(d),Vector()))
            ParamDecl(Some(NamedTypeSpec(NameUser(Week),Vector())),NameDefinition(d),Vector())
              Some(NamedTypeSpec(NameUser(Week),Vector()))
                NamedTypeSpec(NameUser(Week),Vector())
                  NameUser(Week)
                  List()
              NameDefinition(d)
              List()
          Some(NamedTypeSpec(NameUser(Activity),Vector()))
            NamedTypeSpec(NameUser(Activity),Vector())
              NameUser(Activity)
              List()
          ImplementedBody(List(LocalVarDecl(Some(NamedTypeSpec(NameUser(Activity),Vector())),NameDefinition(ret),Vector())),List(JumpLocation(Some(NameDefinitio
            List(LocalVarDecl(Some(NamedTypeSpec(NameUser(Activity),Vector())),NameDefinition(ret),Vector()))
              LocalVarDecl(Some(NamedTypeSpec(NameUser(Activity),Vector())),NameDefinition(ret),Vector())
                Some(NamedTypeSpec(NameUser(Activity),Vector()))
                  NamedTypeSpec(NameUser(Activity),Vector())
                    NameUser(Activity)
                    List()
                NameDefinition(ret)
                List()
            List(JumpLocation(Some(NameDefinition(l2)),Vector(),IfJump(Vector(),Vector(IfThenJump(UnaryExp(!,BinaryExp(&&,BinaryExp(<=,NameExp(NameUser(Mon)),Name
              JumpLocation(Some(NameDefinition(l2)),Vector(),IfJump(Vector(),Vector(IfThenJump(UnaryExp(!,BinaryExp(&&,BinaryExp(<=,NameExp(NameUser(Mon)),NameExp(N
                Some(NameDefinition(l2))
                  NameDefinition(l2)
                List()
                IfJump(Vector(),Vector(IfThenJump(UnaryExp(!,BinaryExp(&&,BinaryExp(<=,NameExp(NameUser(Mon)),NameExp(NameUser(d))),BinaryExp(<=,NameExp(NameUser(d)),
                  List()
                  Vector(IfThenJump(UnaryExp(!,BinaryExp(&&,BinaryExp(<=,NameExp(NameUser(Mon)),NameExp(NameUser(d))),BinaryExp(<=,NameExp(NameUser(d)),NameExp(NameUser
                    IfThenJump(UnaryExp(!,BinaryExp(&&,BinaryExp(<=,NameExp(NameUser(Mon)),NameExp(NameUser(d))),BinaryExp(<=,NameExp(NameUser(d)),NameExp(NameUser(Tue)))
                      UnaryExp(!,BinaryExp(&&,BinaryExp(<=,NameExp(NameUser(Mon)),NameExp(NameUser(d))),BinaryExp(<=,NameExp(NameUser(d)),NameExp(NameUser(Tue)))))
                        BinaryExp(&&,BinaryExp(<=,NameExp(NameUser(Mon)),NameExp(NameUser(d))),BinaryExp(<=,NameExp(NameUser(d)),NameExp(NameUser(Tue))))
                          BinaryExp(<=,NameExp(NameUser(Mon)),NameExp(NameUser(d)))
                            NameExp(NameUser(Mon))
                              NameUser(Mon)
                            NameExp(NameUser(d))
                              NameUser(d)
                          BinaryExp(<=,NameExp(NameUser(d)),NameExp(NameUser(Tue)))
                            NameExp(NameUser(d))
                              NameUser(d)
                            NameExp(NameUser(Tue))
                              NameUser(Tue)
                      List()
                      NameUser(l1)
                  None
              ActionLocation(Some(NameDefinition(l3)),Vector(),AssignAction(Vector(),NameExp(NameUser(ret)),:=,NameExp(NameUser(Work))))
                Some(NameDefinition(l3))
                  NameDefinition(l3)
                List()
                AssignAction(Vector(),NameExp(NameUser(ret)),:=,NameExp(NameUser(Work)))
                  List()
                  NameExp(NameUser(ret))
                    NameUser(ret)
                  NameExp(NameUser(Work))
                    NameUser(Work)
              JumpLocation(Some(NameDefinition(l4)),Vector(),GotoJump(Vector(),NameUser(l0)))
                Some(NameDefinition(l4))
                  NameDefinition(l4)
                List()
                GotoJump(Vector(),NameUser(l0))
                  List()
                  NameUser(l0)
              EmptyLocation(Some(NameDefinition(l1)),Vector())
                Some(NameDefinition(l1))
                  NameDefinition(l1)
                List()
              JumpLocation(Some(NameDefinition(l6)),Vector(),IfJump(Vector(),Vector(IfThenJump(UnaryExp(!,BinaryExp(&&,BinaryExp(<=,CallExp(NameExp(NameUser(attribu
                Some(NameDefinition(l6))
                  NameDefinition(l6)
                List()
                IfJump(Vector(),Vector(IfThenJump(UnaryExp(!,BinaryExp(&&,BinaryExp(<=,CallExp(NameExp(NameUser(attribute__uif__first)),TypeExp(NamedTypeSpec(NameUser
                  List()
                  Vector(IfThenJump(UnaryExp(!,BinaryExp(&&,BinaryExp(<=,CallExp(NameExp(NameUser(attribute__uif__first)),TypeExp(NamedTypeSpec(NameUser(HumpDay),Vector
                    IfThenJump(UnaryExp(!,BinaryExp(&&,BinaryExp(<=,CallExp(NameExp(NameUser(attribute__uif__first)),TypeExp(NamedTypeSpec(NameUser(HumpDay),Vector()))),N
                      UnaryExp(!,BinaryExp(&&,BinaryExp(<=,CallExp(NameExp(NameUser(attribute__uif__first)),TypeExp(NamedTypeSpec(NameUser(HumpDay),Vector()))),NameExp(Name
                        BinaryExp(&&,BinaryExp(<=,CallExp(NameExp(NameUser(attribute__uif__first)),TypeExp(NamedTypeSpec(NameUser(HumpDay),Vector()))),NameExp(NameUser(d))),B
                          BinaryExp(<=,CallExp(NameExp(NameUser(attribute__uif__first)),TypeExp(NamedTypeSpec(NameUser(HumpDay),Vector()))),NameExp(NameUser(d)))
                            CallExp(NameExp(NameUser(attribute__uif__first)),TypeExp(NamedTypeSpec(NameUser(HumpDay),Vector())))
                              NameExp(NameUser(attribute__uif__first))
                                NameUser(attribute__uif__first)
                              TypeExp(NamedTypeSpec(NameUser(HumpDay),Vector()))
                                NamedTypeSpec(NameUser(HumpDay),Vector())
                                  NameUser(HumpDay)
                                  List()
                            NameExp(NameUser(d))
                              NameUser(d)
                          BinaryExp(<=,NameExp(NameUser(d)),CallExp(NameExp(NameUser(attribute__uif__last)),TypeExp(NamedTypeSpec(NameUser(HumpDay),Vector()))))
                            NameExp(NameUser(d))
                              NameUser(d)
                            CallExp(NameExp(NameUser(attribute__uif__last)),TypeExp(NamedTypeSpec(NameUser(HumpDay),Vector())))
                              NameExp(NameUser(attribute__uif__last))
                                NameUser(attribute__uif__last)
                              TypeExp(NamedTypeSpec(NameUser(HumpDay),Vector()))
                                NamedTypeSpec(NameUser(HumpDay),Vector())
                                  NameUser(HumpDay)
                                  List()
                      List()
                      NameUser(l5)
                  None
              ActionLocation(Some(NameDefinition(l7)),Vector(),AssignAction(Vector(),NameExp(NameUser(ret)),:=,NameExp(NameUser(Sleep))))
                Some(NameDefinition(l7))
                  NameDefinition(l7)
                List()
                AssignAction(Vector(),NameExp(NameUser(ret)),:=,NameExp(NameUser(Sleep)))
                  List()
                  NameExp(NameUser(ret))
                    NameUser(ret)
                  NameExp(NameUser(Sleep))
                    NameUser(Sleep)
              JumpLocation(Some(NameDefinition(l8)),Vector(),GotoJump(Vector(),NameUser(l0)))
                Some(NameDefinition(l8))
                  NameDefinition(l8)
                List()
                GotoJump(Vector(),NameUser(l0))
                  List()
                  NameUser(l0)
              EmptyLocation(Some(NameDefinition(l5)),Vector())
                Some(NameDefinition(l5))
                  NameDefinition(l5)
                List()
              JumpLocation(Some(NameDefinition(l10)),Vector(),IfJump(Vector(),Vector(IfThenJump(UnaryExp(!,BinaryExp(==,NameExp(NameUser(d)),NameExp(NameUser(Thur))
                Some(NameDefinition(l10))
                  NameDefinition(l10)
                List()
                IfJump(Vector(),Vector(IfThenJump(UnaryExp(!,BinaryExp(==,NameExp(NameUser(d)),NameExp(NameUser(Thur)))),Vector(),NameUser(l9))),None)
                  List()
                  Vector(IfThenJump(UnaryExp(!,BinaryExp(==,NameExp(NameUser(d)),NameExp(NameUser(Thur)))),Vector(),NameUser(l9)))
                    IfThenJump(UnaryExp(!,BinaryExp(==,NameExp(NameUser(d)),NameExp(NameUser(Thur)))),Vector(),NameUser(l9))
                      UnaryExp(!,BinaryExp(==,NameExp(NameUser(d)),NameExp(NameUser(Thur))))
                        BinaryExp(==,NameExp(NameUser(d)),NameExp(NameUser(Thur)))
                          NameExp(NameUser(d))
                            NameUser(d)
                          NameExp(NameUser(Thur))
                            NameUser(Thur)
                      List()
                      NameUser(l9)
                  None
              ActionLocation(Some(NameDefinition(l11)),Vector(),AssignAction(Vector(),NameExp(NameUser(ret)),:=,NameExp(NameUser(Sleep))))
                Some(NameDefinition(l11))
                  NameDefinition(l11)
                List()
                AssignAction(Vector(),NameExp(NameUser(ret)),:=,NameExp(NameUser(Sleep)))
                  List()
                  NameExp(NameUser(ret))
                    NameUser(ret)
                  NameExp(NameUser(Sleep))
                    NameUser(Sleep)
              JumpLocation(Some(NameDefinition(l12)),Vector(),GotoJump(Vector(),NameUser(l0)))
                Some(NameDefinition(l12))
                  NameDefinition(l12)
                List()
                GotoJump(Vector(),NameUser(l0))
                  List()
                  NameUser(l0)
              EmptyLocation(Some(NameDefinition(l9)),Vector())
                Some(NameDefinition(l9))
                  NameDefinition(l9)
                List()
              JumpLocation(Some(NameDefinition(l14)),Vector(),IfJump(Vector(),Vector(IfThenJump(UnaryExp(!,BinaryExp(||,BinaryExp(==,NameExp(NameUser(d)),NameExp(Na
                Some(NameDefinition(l14))
                  NameDefinition(l14)
                List()
                IfJump(Vector(),Vector(IfThenJump(UnaryExp(!,BinaryExp(||,BinaryExp(==,NameExp(NameUser(d)),NameExp(NameUser(Fri))),BinaryExp(==,NameExp(NameUser(d)),
                  List()
                  Vector(IfThenJump(UnaryExp(!,BinaryExp(||,BinaryExp(==,NameExp(NameUser(d)),NameExp(NameUser(Fri))),BinaryExp(==,NameExp(NameUser(d)),NameExp(NameUser
                    IfThenJump(UnaryExp(!,BinaryExp(||,BinaryExp(==,NameExp(NameUser(d)),NameExp(NameUser(Fri))),BinaryExp(==,NameExp(NameUser(d)),NameExp(NameUser(FriPar
                      UnaryExp(!,BinaryExp(||,BinaryExp(==,NameExp(NameUser(d)),NameExp(NameUser(Fri))),BinaryExp(==,NameExp(NameUser(d)),NameExp(NameUser(FriPart2)))))
                        BinaryExp(||,BinaryExp(==,NameExp(NameUser(d)),NameExp(NameUser(Fri))),BinaryExp(==,NameExp(NameUser(d)),NameExp(NameUser(FriPart2))))
                          BinaryExp(==,NameExp(NameUser(d)),NameExp(NameUser(Fri)))
                            NameExp(NameUser(d))
                              NameUser(d)
                            NameExp(NameUser(Fri))
                              NameUser(Fri)
                          BinaryExp(==,NameExp(NameUser(d)),NameExp(NameUser(FriPart2)))
                            NameExp(NameUser(d))
                              NameUser(d)
                            NameExp(NameUser(FriPart2))
                              NameUser(FriPart2)
                      List()
                      NameUser(l13)
                  None
              ActionLocation(Some(NameDefinition(l15)),Vector(),AssignAction(Vector(),NameExp(NameUser(ret)),:=,NameExp(NameUser(Party))))
                Some(NameDefinition(l15))
                  NameDefinition(l15)
                List()
                AssignAction(Vector(),NameExp(NameUser(ret)),:=,NameExp(NameUser(Party)))
                  List()
                  NameExp(NameUser(ret))
                    NameUser(ret)
                  NameExp(NameUser(Party))
                    NameUser(Party)
              JumpLocation(Some(NameDefinition(l16)),Vector(),GotoJump(Vector(),NameUser(l0)))
                Some(NameDefinition(l16))
                  NameDefinition(l16)
                List()
                GotoJump(Vector(),NameUser(l0))
                  List()
                  NameUser(l0)
              EmptyLocation(Some(NameDefinition(l13)),Vector())
                Some(NameDefinition(l13))
                  NameDefinition(l13)
                List()
              JumpLocation(Some(NameDefinition(l18)),Vector(),IfJump(Vector(),Vector(IfThenJump(UnaryExp(!,BinaryExp(&&,BinaryExp(<=,CallExp(NameExp(NameUser(attrib
                Some(NameDefinition(l18))
                  NameDefinition(l18)
                List()
                IfJump(Vector(),Vector(IfThenJump(UnaryExp(!,BinaryExp(&&,BinaryExp(<=,CallExp(NameExp(NameUser(attribute__uif__first)),TypeExp(NamedTypeSpec(NameUser
                  List()
                  Vector(IfThenJump(UnaryExp(!,BinaryExp(&&,BinaryExp(<=,CallExp(NameExp(NameUser(attribute__uif__first)),TypeExp(NamedTypeSpec(NameUser(WeekEnd),Vector
                    IfThenJump(UnaryExp(!,BinaryExp(&&,BinaryExp(<=,CallExp(NameExp(NameUser(attribute__uif__first)),TypeExp(NamedTypeSpec(NameUser(WeekEnd),Vector()))),N
                      UnaryExp(!,BinaryExp(&&,BinaryExp(<=,CallExp(NameExp(NameUser(attribute__uif__first)),TypeExp(NamedTypeSpec(NameUser(WeekEnd),Vector()))),NameExp(Name
                        BinaryExp(&&,BinaryExp(<=,CallExp(NameExp(NameUser(attribute__uif__first)),TypeExp(NamedTypeSpec(NameUser(WeekEnd),Vector()))),NameExp(NameUser(d))),B
                          BinaryExp(<=,CallExp(NameExp(NameUser(attribute__uif__first)),TypeExp(NamedTypeSpec(NameUser(WeekEnd),Vector()))),NameExp(NameUser(d)))
                            CallExp(NameExp(NameUser(attribute__uif__first)),TypeExp(NamedTypeSpec(NameUser(WeekEnd),Vector())))
                              NameExp(NameUser(attribute__uif__first))
                                NameUser(attribute__uif__first)
                              TypeExp(NamedTypeSpec(NameUser(WeekEnd),Vector()))
                                NamedTypeSpec(NameUser(WeekEnd),Vector())
                                  NameUser(WeekEnd)
                                  List()
                            NameExp(NameUser(d))
                              NameUser(d)
                          BinaryExp(<=,NameExp(NameUser(d)),CallExp(NameExp(NameUser(attribute__uif__last)),TypeExp(NamedTypeSpec(NameUser(WeekEnd),Vector()))))
                            NameExp(NameUser(d))
                              NameUser(d)
                            CallExp(NameExp(NameUser(attribute__uif__last)),TypeExp(NamedTypeSpec(NameUser(WeekEnd),Vector())))
                              NameExp(NameUser(attribute__uif__last))
                                NameUser(attribute__uif__last)
                              TypeExp(NamedTypeSpec(NameUser(WeekEnd),Vector()))
                                NamedTypeSpec(NameUser(WeekEnd),Vector())
                                  NameUser(WeekEnd)
                                  List()
                      List()
                      NameUser(l0)
                  None
              ActionLocation(Some(NameDefinition(l19)),Vector(),AssignAction(Vector(),NameExp(NameUser(ret)),:=,NameExp(NameUser(Drink))))
                Some(NameDefinition(l19))
                  NameDefinition(l19)
                List()
                AssignAction(Vector(),NameExp(NameUser(ret)),:=,NameExp(NameUser(Drink)))
                  List()
                  NameExp(NameUser(ret))
                    NameUser(ret)
                  NameExp(NameUser(Drink))
                    NameUser(Drink)
              EmptyLocation(Some(NameDefinition(l0)),Vector())
                Some(NameDefinition(l0))
                  NameDefinition(l0)
                List()
              JumpLocation(Some(NameDefinition(l20)),Vector(),ReturnJump(Vector(),Some(NameExp(NameUser(ret)))))
                Some(NameDefinition(l20))
                  NameDefinition(l20)
                List()
                ReturnJump(Vector(),Some(NameExp(NameUser(ret))))
                  List()
                  Some(NameExp(NameUser(ret)))
                    NameExp(NameUser(ret))
                      NameUser(ret)
            List()
Model(Some(case_statement_enum.ads),Vector(),Vector(PackageDecl(Some(NameDefinition(Case_Statement_Enum)),Vector(),List(EnumDecl(NameDefinition(Week),
  Some(case_statement_enum.ads)
  List()
  Vector(PackageDecl(Some(NameDefinition(Case_Statement_Enum)),Vector(),List(EnumDecl(NameDefinition(Week),Vector(),List(EnumElement(NameDefinition(Mon)
    PackageDecl(Some(NameDefinition(Case_Statement_Enum)),Vector(),List(EnumDecl(NameDefinition(Week),Vector(),List(EnumElement(NameDefinition(Mon),Vector
      Some(NameDefinition(Case_Statement_Enum))
        NameDefinition(Case_Statement_Enum)
      List()
      List(EnumDecl(NameDefinition(Week),Vector(),List(EnumElement(NameDefinition(Mon),Vector()), EnumElement(NameDefinition(Tue),Vector()), EnumElement(Nam
        EnumDecl(NameDefinition(Week),Vector(),List(EnumElement(NameDefinition(Mon),Vector()), EnumElement(NameDefinition(Tue),Vector()), EnumElement(NameDefi
          NameDefinition(Week)
          List()
          List(EnumElement(NameDefinition(Mon),Vector()), EnumElement(NameDefinition(Tue),Vector()), EnumElement(NameDefinition(Wed),Vector()), EnumElement(Name
            EnumElement(NameDefinition(Mon),Vector())
              NameDefinition(Mon)
              List()
            EnumElement(NameDefinition(Tue),Vector())
              NameDefinition(Tue)
              List()
            EnumElement(NameDefinition(Wed),Vector())
              NameDefinition(Wed)
              List()
            EnumElement(NameDefinition(Thur),Vector())
              NameDefinition(Thur)
              List()
            EnumElement(NameDefinition(Fri),Vector())
              NameDefinition(Fri)
              List()
            EnumElement(NameDefinition(FriPart2),Vector())
              NameDefinition(FriPart2)
              List()
            EnumElement(NameDefinition(Sat),Vector())
              NameDefinition(Sat)
              List()
            EnumElement(NameDefinition(Sun),Vector())
              NameDefinition(Sun)
              List()
        TypeAliasDecl(NameDefinition(WeekDay),Vector(),NamedTypeSpec(NameUser(Week),Vector()))
          NameDefinition(WeekDay)
          List()
          NamedTypeSpec(NameUser(Week),Vector())
            NameUser(Week)
            List()
        TypeAliasDecl(NameDefinition(HumpDay),Vector(),NamedTypeSpec(NameUser(WeekDay),Vector()))
          NameDefinition(HumpDay)
          List()
          NamedTypeSpec(NameUser(WeekDay),Vector())
            NameUser(WeekDay)
            List()
        TypeAliasDecl(NameDefinition(WeekEnd),Vector(),NamedTypeSpec(NameUser(Week),Vector()))
          NameDefinition(WeekEnd)
          List()
          NamedTypeSpec(NameUser(Week),Vector())
            NameUser(Week)
            List()
        EnumDecl(NameDefinition(Activity),Vector(),List(EnumElement(NameDefinition(Work),Vector()), EnumElement(NameDefinition(Sleep),Vector()), EnumElement(N
          NameDefinition(Activity)
          List()
          List(EnumElement(NameDefinition(Work),Vector()), EnumElement(NameDefinition(Sleep),Vector()), EnumElement(NameDefinition(Party),Vector()), EnumElement
            EnumElement(NameDefinition(Work),Vector())
              NameDefinition(Work)
              List()
            EnumElement(NameDefinition(Sleep),Vector())
              NameDefinition(Sleep)
              List()
            EnumElement(NameDefinition(Party),Vector())
              NameDefinition(Party)
              List()
            EnumElement(NameDefinition(Drink),Vector())
              NameDefinition(Drink)
              List()
        ProcedureDecl(NameDefinition(isWorkDay),Vector(),Vector(),List(ParamDecl(Some(NamedTypeSpec(NameUser(Week),Vector())),NameDefinition(d),Vector())),Som
          NameDefinition(isWorkDay)
          List()
          List()
          List(ParamDecl(Some(NamedTypeSpec(NameUser(Week),Vector())),NameDefinition(d),Vector()))
            ParamDecl(Some(NamedTypeSpec(NameUser(Week),Vector())),NameDefinition(d),Vector())
              Some(NamedTypeSpec(NameUser(Week),Vector()))
                NamedTypeSpec(NameUser(Week),Vector())
                  NameUser(Week)
                  List()
              NameDefinition(d)
              List()
          Some(NamedTypeSpec(NameUser(Activity),Vector()))
            NamedTypeSpec(NameUser(Activity),Vector())
              NameUser(Activity)
              List()
          EmptyBody()
Model(Some(fake/standard.ads),Vector(),Vector(PackageDecl(Some(NameDefinition(Standard)),Vector(),List(TypeAliasDecl(NameDefinition(Boolean),Vector(),
  Some(fake/standard.ads)
  List()
  Vector(PackageDecl(Some(NameDefinition(Standard)),Vector(),List(TypeAliasDecl(NameDefinition(Boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List())
    PackageDecl(Some(NameDefinition(Standard)),Vector(),List(TypeAliasDecl(NameDefinition(Boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List())), Type
      Some(NameDefinition(Standard))
        NameDefinition(Standard)
      List()
      List(TypeAliasDecl(NameDefinition(Boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List())), TypeAliasDecl(NameDefinition(Integer),Vector(),NamedType
        TypeAliasDecl(NameDefinition(Boolean),Vector(),NamedTypeSpec(NameUser(Boolean),List()))
          NameDefinition(Boolean)
          List()
          NamedTypeSpec(NameUser(Boolean),List())
            NameUser(Boolean)
            List()
        TypeAliasDecl(NameDefinition(Integer),Vector(),NamedTypeSpec(NameUser(Integer),List()))
          NameDefinition(Integer)
          List()
          NamedTypeSpec(NameUser(Integer),List())
            NameUser(Integer)
            List()
        TypeAliasDecl(NameDefinition(Natural),Vector(),NamedTypeSpec(NameUser(Integer),List()))
          NameDefinition(Natural)
          List()
          NamedTypeSpec(NameUser(Integer),List())
            NameUser(Integer)
            List()
        TypeAliasDecl(NameDefinition(Positive),Vector(),NamedTypeSpec(NameUser(Integer),List()))
          NameDefinition(Positive)
          List()
          NamedTypeSpec(NameUser(Integer),List())
            NameUser(Integer)
            List()
        TypeAliasDecl(NameDefinition(Float),Vector(),NamedTypeSpec(NameUser(Float),List()))
          NameDefinition(Float)
          List()
          NamedTypeSpec(NameUser(Float),List())
            NameUser(Float)
            List()
        TypeAliasDecl(NameDefinition(universal_integer),Vector(),NamedTypeSpec(NameUser(universal_integer),List()))
          NameDefinition(universal_integer)
          List()
          NamedTypeSpec(NameUser(universal_integer),List())
            NameUser(universal_integer)
            List()
        TypeAliasDecl(NameDefinition(universal_real),Vector(),NamedTypeSpec(NameUser(universal_real),List()))
          NameDefinition(universal_real)
          List()
          NamedTypeSpec(NameUser(universal_real),List())
            NameUser(universal_real)
            List()
        TypeAliasDecl(NameDefinition(Character),Vector(),NamedTypeSpec(NameUser(Character),List()))
          NameDefinition(Character)
          List()
          NamedTypeSpec(NameUser(Character),List())
            NameUser(Character)
            List()
        TypeAliasDecl(NameDefinition(String),Vector(),NamedTypeSpec(NameUser(String),List()))
          NameDefinition(String)
          List()
          NamedTypeSpec(NameUser(String),List())
            NameUser(String)
            List()
